{
  "name": "rpigpio",
  "version": "1.0.0",
  "description": "Paramétrage graphique des pins GPIO (mqtt-io) — quoi/où, numéro de pin, inversion, direction — et déploiement du config.yaml généré + redémarrage du service distant",
  "main": "dist/domain/index.js",
  "scripts": {
    "build": "tsc -b",
    "build:ui": "tsc -p src/presentation/tsconfig.ui.json",
    "watch": "tsc -w"
  },
  "dependencies": {
    "js-yaml": "^4.3.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
