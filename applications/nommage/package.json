{
  "name": "nommage",
  "version": "1.0.0",
  "description": "Application de gestion des conventions de nommage et taxonomie pour Home Assistant via MQTT Discovery",
  "main": "dist/domain/index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc -b && mkdir -p dist/defaultconfig && cp -r src/defaultconfig/. dist/defaultconfig/",
    "build:ui": "tsc -p src/presentation/tsconfig.ui.json",
    "watch": "tsc -w"
  },
  "dependencies": {
    "js-yaml": "^4.3.1",
    "mqtt": "^5.0.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/mqtt": "^2.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
