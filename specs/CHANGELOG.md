# CHANGELOG - Historique des Spécifications

**Dernière mise à jour :** 24 Septembre 2026

---

## 📅 Historique Global

### 📌 Spécifications Socle

| Date | Version | Fichier | Auteur | Changements |
|------|---------|---------|--------|-------------|
| 19/09/2026 | **v6.0** | fonctionnelles-rfxcom_specs_v6.0.md | Claude | **Fusion** de `fonctionnelles-rfxcom_specs_v5.17.md` + `implementation-rfxcom_specs_v1.5.md` + `recepteurs-emetteurs-rfxcom_specs_v5.4.md` en un seul document, dernier des trois chantiers de fusion de la session (après arbreouquoi et nommage ci-dessous). Trois parties : "Partie 1 — Fonctionnel" (§1-§22, inchangée), "Partie 2 — Technique/Implémentation" (§T1-§T14, préfixe T), "Partie 3 — Récepteurs, Émetteurs et Scènes" (§R1-§R12, préfixe R) — cette troisième partie reste distincte malgré le recoupement de sujet avec la Partie 1 (nomenclature, architecture récepteurs/émetteurs) car elle apporte un niveau de détail technique non dupliqué ailleurs (schéma Zod complet, interfaces TypeScript, payloads MQTT Discovery par type d'entité, checklist d'implémentation) et reste activement citée par nom de fichier + section dans au moins 8 fichiers TypeScript réels d'`applications/rfxcom/src/domain/` (références de code devenues obsolètes, correction volontairement hors périmètre — changement de masse sur du code). Aucun contenu `InterAppClient` mort à retirer (déjà propre dans les 3 documents sources). Deux notes de correction ajoutées sans modifier le texte original : Partie 2 §T8.1 et Partie 3 §R4.3 décrivaient la perte de `lastOn`/`lastLevel`/`lastValue` au rechargement comme non corrigée, alors qu'un correctif réel est intervenu depuis (15/08/2026, voir Partie 1 §9.2/§9.2bis/§9.2ter/§20) sans que ces deux sections plus anciennes n'en soient informées. **`classification-rfxcom_specs_v1.0.md` volontairement NON fusionné** : contrairement au cas nommage (exclusion pour cause de références externes), ce document RFXCOM-spécifique décrit un mécanisme de classification (`HaClassifier` à cascade de priorité, multi-QUOI, `non_classifie`, `isDimmable` forçant le QUOI du récepteur) qui ne correspond pas au mécanisme réellement implémenté (`classification.ts::determineQuoi()`, une simple table sans cascade ni multi-QUOI, déjà documentée fidèlement en Partie 1 §9.3/Partie 2 §T5.1) ni au classifieur générique réel du socle (`TaxonomyHaClassifier`, basé sur la chaîne de taxonomie, pas sur `subType`/`device_class`). **Décision utilisateur (19/09/2026, suite à clarification demandée) : archivé comme conception jamais construite**, à la manière de l'ancien `inter-app-communication_specs`. Références corrigées dans `PROMPT.md`/`PROMPT_PROJET.md` (table de correspondance RFXCOM) et `techniques-socle-ha-mqtt_specs` §8.3 (bandeau ajouté, signalant au passage que §8.3.1 lui-même documente le même mécanisme non vérifié contre le réel, non corrigé — hors périmètre). Anciennes versions archivées (`specs/archives/v5.17-rfxcom/`, y compris `classification-rfxcom_specs_v1.0.md` lui-même). |
| 19/09/2026 | **v2.0** | fonctionnelles-nommage_specs_v2.0.md | Claude | **Fusion** de `fonctionnelles-nommage_specs_v1.8.md` + `implementation-nommage_specs_v1.8.md` en un seul document, même patron que la fusion pilote arbreouquoi ci-dessous : "Partie 1 — Fonctionnel" (§1-§10, inchangée) et "Partie 2 — Technique/Implémentation" (§T1-§T8, préfixe T). Les deux anciens documents avaient chacun, en doublon et en fin de fichier (après leur propre tableau "Historique des Versions", réutilisant à tort le numéro `9` déjà pris par "Exemples d'Utilisation"), une section "Communication Inter-Applications" massive (~400 lignes chacune, ~800 au total) décrivant des capacités `InterAppClient` jamais implémentées — retirée, remplacée par un court paragraphe unique pointant vers `techniques-socle-ha-mqtt_specs` §9bis. Deux renvois croisés laissés "à préciser côté implémentation" dans la v1.8 fonctionnelle (§3.4, §7.4) sont désormais résolus vers des sections internes réelles (§T4.1/§T4.2.2). **`nommage_specs_v1.0.md` volontairement NON fusionné** (déviation par rapport à la demande initiale, tranchée après vérification) : ce fichier est un protocole de taxonomie générique référencé par 9 documents à travers le dépôt (guide-nouvelle-application, arbreouquoi, teleinfo, rpigpio, README de l'app nommage elle-même...), pas seulement par l'application NOMMAGE qui ne fait que l'implémenter côté MQTT — le fusionner aurait cassé ces références externes et conflaté deux notions distinctes. Reste un document séparé et immuable comme avant. Anciennes versions v1.7/v1.8 archivées (`specs/archives/v1.8-nommage/`). |
| 19/09/2026 | **v2.0** | fonctionnelles-arbreouquoi_specs_v2.0.md | Claude | **Fusion** de `fonctionnelles-arbreouquoi_specs_v1.5.md` + `implementation-arbreouquoi_specs_v1.4.md` en un seul document (demande explicite utilisateur : "il y a plusieurs specs qui traitent de... pareil pour arbreouquoi... il faudra bien les séparer") — "Partie 1 — Fonctionnel" (§1-§9, inchangée) et "Partie 2 — Technique/Implémentation" (§T1-§T10, préfixe T pour rester distincte sans tout renuméroter) clairement séparées dans le même fichier. Les deux anciens documents avaient chacun, en doublon, une section "Communication Inter-Applications" massive (~340 lignes chacune, ~680 au total) décrivant des capacités `InterAppClient` jamais implémentées — retirée, remplacée par un court paragraphe unique en fin de document pointant vers `techniques-socle-ha-mqtt_specs` §9bis. Références croisées internes mises à jour, §T9/§T10 (build/déploiement) corrigés vers les mécanismes réels actuels. Anciennes versions v1.4 et v1.5 archivées. Pilote de la restructuration demandée pour nommage et rfxcom (à suivre). |
| 19/09/2026 | **v2.0** | guide-nouvelle-application_specs_v2.0.md | Claude | **Réécriture complète** (demande explicite utilisateur, "il faut absolument la mettre à jour") — v1.11 restait marquée obsolète depuis le 08/09/2026 sans jamais avoir été corrigée. Confirmé par recherche exhaustive dans le dépôt (zéro occurrence) : `domain/capabilities.ts`/`ApplicationCapabilities`/`InterAppClient`/le pattern Request/Reply de tout le §11 v1.11 n'ont **jamais été implémentés** — retirés. Import direct de `SocketService` côté client (§3.9 v1.11) également faux dans la pratique — remplacé par le vrai pattern `window.app.socketService.getSocket()`, vérifié dans plusieurs apps réelles (arexx/teleinfo/outils). Reconstruit entièrement à partir du code réel : structure vérifiée (exemple `outils`), `domain/index.ts`/`{App}Service.ts`/`standalone.ts` avec exemples issus d'applications existantes, process séparé vs in-process clarifié, `HaBridgeClient`, déploiement SSH/`targets[]`, convention systemd/cron auto-descriptive (reprise de v1.11, qui restait valable), routes HTTP génériques upload/download (nouvelles, ajoutées avec l'app `outils`). Ancienne version v1.11 archivée. |
| 19/09/2026 | **v1.1 (RETIRÉE)** | inter-app-communication_specs_v1.1.md | Claude | Se présentait comme "Spécification active" depuis le 20/07/2026 alors qu'aucun code du dépôt n'implémente `InterAppClient`/`ApplicationCapabilities` (recherche exhaustive, zéro résultat) — d'abord marquée non implémentée (bandeau d'avertissement), puis **retirée de `specs/current/`** le même jour sur demande explicite ("on supprime l'ancienne spec sur la communication") une fois son contenu réel couvert ailleurs (`techniques-socle-ha-mqtt_specs` §9bis + `guide-nouvelle-application_specs` §3.2bis). Archivée pour l'historique (v1.0 et v1.1). |
| 19/09/2026 | **7 fichiers** | implementation-nommage v1.8, fonctionnelles-arbreouquoi v1.5, fonctionnelles-nommage v1.8, implementation-arbreouquoi v1.4, fonctionnelles-espdisplay v1.2, fonctionnelles-supervisor v2.9, fonctionnelles-rfxcom v5.17 | Claude | **Références mortes corrigées** (demande explicite, "oui en faisant référence au core") — chacun pointait vers `inter-app-communication_specs_v1.0.md`, retirée de `specs/current/` (voir v1.1 ci-dessus). 4 d'entre eux (nommage×2, arbreouquoi×2) décrivaient aussi à tort l'usage d'`InterAppClient` (jamais implémenté) — corrigé vers le mécanisme réel : `eventBus.emitGeneric()`/`onGeneric()` (Fire & Forget) et `CorrelatedRequester` (Request/Reply), tous deux documentés dans `techniques-socle-ha-mqtt_specs` §9bis. `fonctionnelles-supervisor` corrige en prime deux autres pointeurs de version obsolètes trouvés au passage (`techniques-socle-ha-mqtt_specs_v4.30`→`v4.33`, `fonctionnelles-espdisplay_specs_v1.1`→`v1.2`). Anciennes versions archivées. |
| 19/09/2026 | **v4.33** | techniques-socle-ha-mqtt_specs_v4.33.md | Claude | **§9bis ajoutée — communication générique inter-modules (Fire & Forget + Request/Reply)**, demande explicite utilisateur ("doit être décrit... dans le core puisque c'est lui qui en assume le transport"). Documente le mécanisme RÉEL : `emitGeneric`/`onGeneric`/`offGeneric` sur `IEventBus` (in-process `EventBus` ou inter-process `IpcEventBus`, transparents l'un pour l'autre) pour le Fire & Forget, `CorrelatedRequester` (centralisé le 24/08/2026) pour le Request/Reply corrélé — utilisé réellement par le dialogue `ia`↔`planificateur` (`planificateur:deploy`/`:reply`) et `HaBridgeClient`. **§5.5 "Communication Inter-Applications"** (même contenu mort que l'ancien `inter-app-communication_specs`, dupliqué ici depuis v4.7) **vidée**, remplacée par un pointeur vers §9bis. `guide-nouvelle-application_specs_v2.0.md` §3.2bis détaille désormais le code réel des deux process (demandeur/répondeur, exemple `planificateur`↔`ia`) plutôt qu'un simple pointeur. Contient aussi (**v4.32**, même journée) : `Dockerfile` étend sa copie à `scripts/`+`compose.deploy.yaml` (en plus de `applications/`), nécessaire à l'app `outils` (archive auto-extractible, `BundleBuilder.ts`), jamais documenté au moment du changement (18/09/2026). Anciennes versions v4.31 et v4.32 archivées. |
| 25/09/2026 | **v1.2** | fonctionnelles-supervision_specs_v1.2.md | Claude | Sauvegardes lues par SSH (`status.json`, marqueurs de dernière réussite par dossier et cadence, script/cron installés) — jamais Nextcloud, faute de mot de passe dans dimotic-ha (option A retenue) ; « aucune sauvegarde » et « injoignable » signalés. v1.1 archivée. |
| 25/09/2026 | **v1.6** | fonctionnelles-sauvegarde_specs_v1.6.md | Claude | Marqueurs `last-<dossier>-<cadence>` et `cadencesEchec` dans le script (§5ter), échange `sauvegarde:supervision:status` (§5quater) ; trou constaté : une machine restaurée n'est plus sauvegardée (ha2 depuis le 24/09) — « Pousser » à intégrer à l'assistant de restauration. v1.5 archivée. |
| 25/09/2026 | **v1.1** | fonctionnelles-supervision_specs_v1.1.md | Claude | Décisions utilisateur : affichage et sélection **sur la page d'accueil** (plus de page de paramétrage, diffusion immédiate) ; logique entièrement dans l'application, le core ne fournit qu'un emplacement qui charge son affichage ; application **activée d'office** (`enabledByDefault`, exception à la règle « nouvelle application désactivée ») ; topic LWT corrigé (`app/+/status` filtré). v1.0 archivée. |
| 25/09/2026 | **v1.1** | fonctionnelles-outils_specs_v1.1.md | Claude | **Exécution par SSH** à côté du téléchargement (décisions utilisateur) : machines du gossip + locale + saisie libre, clé de dimotic-ha avec prérequis `ssh-copy-id` affiché, sortie en direct et réponses tapées (pseudo-terminal), champ `execution` du `.yaml` ; sécurisation reportée (TODO). v1.0 archivée. |
| 25/09/2026 | **v1.0** | fonctionnelles-supervision_specs_v1.0.md | Claude | **Nouvelle application SUPERVISION — conception validée, aucun code** : inventaire des machines/applications du site par gossip (`dimotic/core/+/known-apps`), sélection des éléments supervisés diffusée (message retenu `dimotic/supervision/selection`, la plus récente l'emporte, copie locale), état des sauvegardes en face de chaque machine demandé à l'application sauvegarde (Nextcloud + `status.json` par SSH, correspondance par adresse IP, seuils 2 j `journalier` / 8 j `hebdomadaire`), présence des machines par LWT (~90 s), annonce du core enrichie de l'état des process et republiée à chaque changement + toutes les 5 min (modification de `AppGossipService`), site courant uniquement, actions reportées. |
| 25/09/2026 | **v1.1** | fonctionnelles-supervision-externe_specs_v1.1.md | Claude | Renvoi : la première version concrète est l'application SUPERVISION (`fonctionnelles-supervision_specs_v1.0.md`) ; actions et nœuds RPi indépendants restent la suite. v1.0 archivée. |
| 25/09/2026 | **v1.0** | fonctionnelles-outils_specs_v1.0.md | Claude | **Première spécification formelle** d'OUTILS (bibliothèque de scripts shell auto-descriptifs) : stockage en triplets sur deux racines (intégrée/ajoutée), directives `@outils:hint/select/checklist/default/bundle`, trois formes de téléchargement, ajout/import/suppression, contrôle des identifiants et chemins, scripts intégrés dont `build-all` (nouveau) et `commit-push-docker` (compilation de vérification avant tag, version mineure par défaut). `PROMPT_PROJET.md` v1.13 : table §11 remise à jour — lignes OUTILS, SAUVEGARDE et SUPERVISION-EXTERNE ajoutées, lignes NOMMAGE/ARBREOUQUOI (specs `implementation-*` fusionnées), ESPDISPLAY et « Communication Inter-Applications » (`inter-app-communication_specs` retirée → socle §9bis) et RFXCOM corrigées ; ordre de lecture et arborescence de `specs/current/` corrigés. |
| 24/09/2026 | **v1.8** | fonctionnelles-haplan_specs_v1.8.md | Claude | Revue de code : états HA en direct même si HA n'était pas prêt au démarrage, délais d'expiration des déploiements écran/Lovelace. v1.7 archivée. |
| 24/09/2026 | **v1.3** | fonctionnelles-scriptsha_specs_v1.3.md | Claude | Revue de code : délai d'expiration sur les requêtes vers le core (verrou de réconciliation bloqué à vie sinon), dépôt raté annulé. v1.2 archivée. |
| 24/09/2026 | **v2.1** | fonctionnelles-nommage_specs_v2.1.md | Claude | **Plus de sources MQTT propres** (décision utilisateur) : écoute des préfixes via la connexion MQTT du socle, config réduite à `prefixes` (migration automatique), retrait à la source propagé à HA, taxonomies par topic, rejeu au retour de HA, config relue. v2.0 archivée. |
| 24/09/2026 | **v1.5** | fonctionnelles-rpigpio_specs_v1.5.md | Claude | Revue de code : GPIO unique, présence reconnectée sur changement MQTT, `bridgeInstance` figé à chaud, mémoire alignée sur le disque. v1.4 archivée. |
| 24/09/2026 | **v1.4** | fonctionnelles-teleinfo_specs_v1.4.md | Claude | Revue de code : présence reconnectée sur changement MQTT, ADCO unique, mémoire alignée sur le disque. v1.3 archivée. |
| 24/09/2026 | **v6.2** | fonctionnelles-rfxcom_specs_v6.2.md | Claude | Volets : dernière position connue republiée vers HA au démarrage (jamais la position par défaut, jamais de commande matérielle). v6.1 archivée. |
| 24/09/2026 | **v1.6** | fonctionnelles-arexx_specs_v1.6.md | Claude | Revue de code : config relue sur disque (cibles périmées), acquisition relancée à chaud, `bridgeInstance` non modifié à chaud, entité HA retirée à la désélection/suppression. v1.5 archivée. |
| 24/09/2026 | **v1.4** | fonctionnelles-evoo7_specs_v1.4.md | Claude | Revue de code : config relue sur disque avant reconnexion à chaud du boîtier, `bridgeInstance` non modifié à chaud, thermostat publié après enregistrement réussi. v1.3 archivée. |
| 24/09/2026 | **v6.1** | fonctionnelles-rfxcom_specs_v6.1.md | Claude | **Derniers états sortis de la configuration** (décision utilisateur) : `rfxcom-derniers-etats.json`, écriture groupée ≤ 1/30 s + à l'arrêt, migration automatique — la config n'est plus réécrite à chaque trame RF (~3 600/jour mesurées sur stfort). Reconnexion à chaud du port série réparée (config relue), changement de type de récepteur = suppression + recréation côté HA, arrêt propre de la boucle de reconnexion. v6.0 archivée. |
| 24/09/2026 | **v1.11** | fonctionnelles-planificateur_specs_v1.11.md | Claude | **Refonte du déclenchement** (décision utilisateur) : plus de réinterprétation de la phrase par Mistral au déclenchement ni de cache de résolution — la structure décidée à la création est exécutée et recalculée en code (entités, attentes, macros, conditions : soleil/numérique/état en code, sinon vrai/faux demandé à ia via `planificateur:condition`) ; « éteins-la » cible l'entité déclenchante ; **repli vers l'agent de conversation HA supprimé** (cet agent est ia : boucle). Revue de code : validation à la réception (entrée invalide qui empoisonnait toutes les écritures), activer/modifier recalculent l'échéance, recréation désarme l'ancienne, state_change sur vrai passage d'état, reprises après ha:ready, window (jours + fin inverse), duration (immédiate + fin), sun (attente position GPS + nouvel essai), macro dite exécutée. v1.10 archivée. |
| 24/09/2026 | **v1.14** | fonctionnelles-ia_specs_v1.14.md | Claude | **Revue de code — cache de phrases** (§16.10) : `DeployResponder` sans cache (collision avec les phrases dites via HA : séquence de déclenchement rejouée comme commande immédiate) ; déclencheurs `date` et actions en échec jamais mis en cache ; `phrase_originale` complétée pour les planifications de l'interpréteur (était vide) ; rechargement à chaud par surveillance du dossier `data/ia/` (fs.watch sur fichier sourd après rename) ; port Ollama occupé sans crash ; modèles intégrés depuis `__dirname` ; échappement HTML ; **démarrage subordonné au premier `ha:ready`** (serveur Ollama, test, réinterprétations) ; **`DeployResponder` supprimé** (plus de réinterprétation au déclenchement), remplacé par `ConditionEvaluator` (vrai/faux) ; règles : verbe/quoi obligatoires dans toute action. v1.13 archivée. |
| 24/09/2026 | **v2.10** | fonctionnelles-supervisor_specs_v2.10.md | Claude | **§8 réécrit — cycle de vie des applications** : jamais de redémarrage du core pour une application ; un seul chemin activer/désactiver (`registerApp`/`activateApp`/`deactivateApp`) ; application nouvelle = désactivée (`knownApps`, installation neuve comprise) ; deux racines `applications/` + `data/applications/` (lien relatif vers le core) ; nettoyage MQTT par le core à toute fin de process + `availability_topic` dans la découverte HA ; états `restarting`/`crashed` visibles + Relancer ; §8.7 application de test `testcycle`. Validé en réel le 24/09. v2.9 archivée dans `archives/v2.9-supervisor/`. |
| 24/09/2026 | **v2.1** | guide-nouvelle-application_specs_v2.1.md | Claude | **§9 mis à jour** (cycle de vie à chaud, application nouvelle désactivée, racine externe, `testcycle`). v2.0 archivée dans `archives/v2.0-guide/`. |
| 24/09/2026 | **v1.5** | fonctionnelles-sauvegarde_specs_v1.5.md | Claude | **§6ter amendé** — premier remplacement réel réussi (ha2 RPi4 → Orange Pi 4 Pro restauré depuis Nextcloud, IP .51 reprise par DHCP, tout redémarre) ; `docker compose pull` des éléments restaurés dès l'étape 1 (images absentes sur machine neuve, qui rallongeaient la coupure à l'étape 2) ; constats du test consignés (dongle absent → échec définitif à revoir, étape 2 non lançable directement en .51). v1.4 archivée dans `archives/v1.4-sauvegarde/`. |
| 23/09/2026 | **v1.4** | fonctionnelles-sauvegarde_specs_v1.4.md | Claude | **§5bis/§6/§8 amendés** — corrections issues du premier test en conditions réelles (ha2 + vrai Nextcloud, sauvegarde `stfort/ha2/journalier/docker-2026-09-23.tar.gz` 1,6 Go réussie) : bouton « Importer depuis le gossip » et import automatique au démarrage **supprimés** (source inadaptée : cibles de déploiement périmées, sans site, parfois `127.0.0.1`) ; « Pousser »/« Lancer maintenant » utilisent la ligne et la section Nextcloud **à l'écran**, « Pousser » enregistre la machine en cas de succès ; mot de passe d'application visible à la saisie ; id de machine recalculé tant que la ligne est nouvelle (bug id `-`) ; timeout « Lancer maintenant » 5 → 15 min. Fuite du mot de passe dans les logs serveur/broadcast Socket.io laissée en l'état (décision explicite). **Complété le même jour (avant toute publication)** : §4 tailles décompressées dans le manifeste (`nom<TAB>octets`), §5ter log d'erreurs de `tar` conservé si code ≠ 0 et code 1 toléré, **§6ter remplacé — restauration conçue et implémentée** (la machine de remplacement reprend l'IP de la source, pas de réécriture d'adresse ; pilotage depuis n'importe quel dimotic-ha vers une destination SSH root ; assistant 6 écrans ; script détaché en 2 étapes, étape 1 sans aucun redémarrage, existant déplacé dans `/<parent>-backup-<horodatage>/`, dimotic-ha démarré en dernier). v1.3 archivée dans `archives/v1.3-sauvegarde/`. |
| 18/09/2026 | **v1.11** | guide-nouvelle-application_specs_v1.11.md | Claude | **Nouvelle convention notée** (pas une refonte du guide, toujours marqué obsolète) : une application non-Docker (systemd/cron) sous `/dimotic-ha-addons/<nom>/` doit désormais placer son fichier d'activation (`<nom>.service` ou `<nom>.cron`, format cron.d avec colonne utilisateur) **dans son propre répertoire de déploiement**, installé par lien symbolique vers `/etc/systemd/system/`/`/etc/cron.d/` plutôt qu'écrit directement là — décidé en concevant la restauration de `fonctionnelles-sauvegarde_specs` (§6ter) : une seule source de vérité par app, sauvegardée avec le reste, aucun nom de service à deviner/collecter séparément à la restauration. ⚠️ Piège documenté : le nom du lien dans `/etc/cron.d/` ne doit contenir aucun point (Debian ignore silencieusement les fichiers pointés sinon). `teleinfo/DeployService.ts` reste à corriger pour suivre cette convention (pas encore fait). Ancienne version v1.10 archivée. |
| 18/09/2026 | **v1.3** | fonctionnelles-sauvegarde_specs_v1.3.md | Claude | **§4/§4quater/§5/§5bis/§6/§6bis amendés, §6ter ajouté** — refonte issue de l'implémentation réelle du chantier A (script de sauvegarde, jamais écrit avant cette version) et de la conception de la restauration (chantier B, tranche 2). **§4** : l'unité de sauvegarde n'est plus le répertoire de déploiement individuel mais `/docker/`et `/dimotic-ha-addons/` **pris entiers** (demande explicite, "sans s'occuper de ce qu'il y a dedans"), avec un manifeste daté poussé à côté de chaque archive. **§4quater** : arborescence Nextcloud refondue — `<rootPath>/<site>/<machine>/journalier\|hebdomadaire/<parent>-AAAA-MM-JJ.tar.gz` (+ manifeste), remplace complètement le modèle "courant + mensuel 12 mois glissants" par une rotation 10 jours/10 semaines glissante ; détection de changement abandonnée pour cette version (poussée inconditionnelle sur calendrier fixe). **§5/§5bis** : cron fixé à 3h05 quotidien (décalé pour ne pas croiser des automatisations d'extinction de lumières existantes), déploiement du script greffé sur le bouton « Pousser » existant (`ScriptPushService`) plutôt qu'un mécanisme externe à dimotic-ha comme envisagé à l'origine — nouveau parent `/dimotic-backup/` (script + statut + log cron, jamais sauvegardé) ; nouveau bouton « Lancer maintenant » (déclenchement à la demande) et variables d'environnement de test (`RETENTION_DAYS`/`WEEKLY_DOW`/`TODAY`/`DOW` surchargeables) ajoutés sur demande explicite. **§6** : le mot de passe + script + cron se poussent désormais ensemble en un clic depuis Paramètres Techniques (plus le tableau de bord, entièrement délesté) ; `id` des machines n'est plus saisi à la main (dérivé de site+machine) ; `host` requis. **§6ter (nouveau)** : première conception concrète de l'assistant de restauration — granularité application unique possible via le manifeste, arrêt/redémarrage `docker compose` (pas de nom de conteneur à connaître, vérifié dans le code réel) pour `/docker/`, convention systemd/cron auto-descriptive pour `/dimotic-ha-addons/` (voir `guide-nouvelle-application_specs_v1.11.md`), boucle simple pour une restauration multi-application. Risque SQLite (§4bis) : mitigation envisagée en v1.0 non implémentée dans le script réel, assumé pour cette version. Ancienne version v1.2 archivée. |
| 17/09/2026 | **v1.2** | fonctionnelles-sauvegarde_specs_v1.2.md | Claude | **§3bis/§4quater/§6/§8 amendés** — trois simplifications successives du schéma de configuration pendant l'implémentation de l'app `sauvegarde` ont fait disparaître le champ `deploymentType` (`'docker'\|'raw'`) : une machine couverte n'est plus qu'une ligne `{id, site, machine, host}`, plus de distinction de type à saisir. **§4quater** : la correspondance directe `deploymentType`→dossier Nextcloud (`docker`/`dimotic-ha-addons`) décrite en v1.1 n'existe donc plus côté config — c'est désormais le futur script hôte (chantier A, toujours non commencé) qui la détermine lui-même en testant l'existence des deux répertoires sur la machine, l'arborescence Nextcloud elle-même restant inchangée dans sa forme. **§3bis** : le chemin du secret Nextcloud par machine devient **fixe** (`/dimotic-secrets/nextcloud-backup`, nouveau troisième parent dédié à la racine aux côtés de `/docker/` et `/dimotic-ha-addons/`) au lieu d'un exemple sous `/docker/.secrets/` référencé par un champ config — plus de champ de référence à saisir du tout. **§6** : demande explicite de l'utilisateur, la poussée du mot de passe par machine (SSH, sans terminal) et son tag persistant (`secretDeployed`) migrent du formulaire de configuration vers le **tableau de bord**, une ligne par machine avec son propre champ mot de passe/bouton/tag. Import assisté depuis le gossip (bouton dédié, n'écrase jamais une entrée existante) inchangé dans son principe, adapté au nouveau schéma à plat. Vérifié en conditions réelles (UI + circuit socket + tentative SSH réelle vers une machine du parc). Ancienne version v1.1 archivée. |
| 17/09/2026 | **v1.1** | fonctionnelles-sauvegarde_specs_v1.1.md | Claude | **§4quater ajouté** (amende §4) : un niveau intermédiaire `docker/` ou `dimotic-ha-addons/` s'intercale désormais entre `<machine>/` et `<répertoire-de-déploiement>/` dans l'arborescence Nextcloud, reprenant tel quel le nom réel du parent de déploiement (§4ter) — demande explicite de l'utilisateur pendant l'implémentation, pour garder la sauvegarde alignée sur la vraie convention disque et faciliter une évolution future (nouveau parent = nouveau dossier, sans repenser la structure). Correspond exactement au champ `deploymentType` (`'docker'\|'raw'`) déjà présent dans `sauvegardeTargetSchema`. §8 (plan de mise en œuvre) mis à jour : chantier B (app dimotic-ha) commencé — squelette livré (`applications/sauvegarde/`, config Nextcloud + `targets[]` + import assisté par gossip + poussée du secret par SSH), `NextcloudWebDavClient`/restauration réelle restent à faire. Reste inchangé pour §1-§3ter/§4/§4bis/§4ter/§5-§5ter/§6-§7. Ancienne version v1.0 archivée. |
| 16/09/2026 | **v1.0** | fonctionnelles-supervision-externe_specs_v1.0.md | Claude | **Nouvelle spec — supervision externe indépendante + alerte**, conception seulement (aucun code). Extrait `TODO.md` ("🟡 Supervision multi-sites indépendante + alerte hors HA") dans une spec dédiée, distincte de `fonctionnelles-supervisor_specs_v2.8.md` (registre de présence interne, réutilisé comme brique) — le vrai besoin étant d'alerter même quand HA (donc la voie normale d'alerte) est en panne. Conception : 2 RPi1 "sacrifiables" (un par site stfort/noisy, chacun surveillant les deux sites via `TargetGossipService`/WireGuard déjà en place), pattern agent minimal repris de l'agent `teleinfo` (§11.5 socle) mais rôle inversé (écoute au lieu de publier), canal SMS/email de repli (legacy `zdidnodesmsusb`/`zdidnodedomomail`, confirmés actifs sur noisy). **Surveillance des sauvegardes intégrée dès la conception initiale** (§4) : consomme le marqueur de statut local ajouté à `fonctionnelles-sauvegarde_specs_v1.0.md` §5ter, alerte sur échec explicite ou absence/péremption. Restent ouverts : sondage zigbee2mqtt/autres, accès exact au marqueur de statut, surveillance mutuelle des deux RPi entre eux. |
| 16/09/2026 | **v1.0** | fonctionnelles-sauvegarde_specs_v1.0.md | Claude | **Nouvelle spec — sauvegarde/restauration**, conception complète (aucun code écrit), relue et affinée point par point par l'utilisateur après un premier jet. Extrait le point de résilience laissé ouvert dans `fonctionnelles-supervisor_specs_v2.8.md` §6.4 (suivi jusqu'ici uniquement dans `TODO.md`) dans une spec dédiée. Destination (Nextcloud auto-hébergé, déjà dupliqué offsite), transport (push WebDAV/`curl` par machine via WireGuard, marche même sur le RPi1 le plus contraint), remplacement complet (pas de différentiel construit). **Unité de sauvegarde reconçue et généralisée en cours de discussion** : le **répertoire de déploiement** entier (Docker ou non — couvre aussi teleinfo sur RPi1, pas Docker du tout), identifié par son nom réel plutôt que par une catégorie HA/dimotic-ha/externe — couvre nativement les instances multiples du même logiciel. **Gestion des mots de passe Nextcloud** précisée (§3bis) : fichier hôte dédié hors de tout répertoire sauvegardé, jamais la valeur brute dans la config de l'app. **Risque de snapshot SQLite incohérent résolu** (pas seulement accepté) via `sqlite3 .backup`, combiné à une fenêtre nocturne de faible activité pour la fréquence. Deux cadences (courant écrasé + mensuel daté 12 mois glissants, purge déterministe). Script de création/poussée volontairement indépendant de dimotic-ha, tourne au niveau de l'hôte, déployé en se greffant sur un mécanisme de poussée existant, avec vérification d'intégrité et marqueur de statut (§5ter). Restauration confiée à une **nouvelle application dimotic-ha dédiée, nommée « Sauvegarde/Restauration »** — configuration + restauration pilotée par l'interface, choix source/cible/date indépendants pour une restauration croisée entre machines, et rôle explicite de mécanisme de reprise après sinistre lié au futur pipeline de provisionnement (§6bis). **Emplacements de déploiement harmonisés** (§4ter, vérifié en direct sur stfort et noisy) : `/docker/<app>/` déjà cohérent pour tout ce qui est Docker (rpigpio inclus, confirmé par `docker inspect`), nouveau parent `/dimotic-ha-addons/<app>/` pour les agents non-Docker dispersés (teleinfo `/opt/`, arexx `/root/`) — nouveau défaut pour les futures cibles seulement, pas de migration rétroactive, implémentation hors périmètre de cette spec. |
| 08/09/2026 | **v1.10** | guide-nouvelle-application_specs_v1.10.md | Claude | **Marqué OBSOLÈTE** (avertissement en tête de document, pas de réécriture du contenu) : daté du 04/08/2026, ne couvre aucun des changements d'architecture devenus standard depuis (migration process séparé mi-août→24/08, `HaBridgeClient`, `bridgedEvents`/`computeBridgeInstance()`/`DIMOTIC_MACHINE_ID`, `disabledApps` comme seul vrai interrupteur d'activation, cibles multi-machines `targets[]`/`haStackTargets`, registre gossip) — suivre ce guide tel quel produirait une application construite à l'ancienne. Mise à jour complète du contenu différée, demandée explicitement par l'utilisateur pour plus tard. Ancienne version v1.9 archivée. |
| 08/09/2026 | **v1.7** | fonctionnelles-nommage_specs_v1.7.md, implementation-nommage_specs_v1.7.md | Claude | **`sources[].mqtt.host`/`clientId` corrigés**, en travaillant la conception "duplication config multi-machines" : `host` avait `127.0.0.1` en conditions réelles pour la source `ha2.local` (cassait tout hors du cas "même machine que le broker"), corrigé en l'IP LAN réelle + `hint` d'avertissement permanent ajouté à l'UI. `clientId` n'est plus qu'un préfixe — `NommageMqttIntegrationService.connectSource()` calcule désormais l'identifiant de connexion via `computeBridgeInstance(clientId, DIMOTIC_MACHINE_ID)`, même mécanisme que `bridgeInstance` (arexx/evoo7/rfxcom/rpigpio) — l'unicité globale entre machines n'est plus à la charge de l'utilisateur. Anciennes versions v1.6 archivées. |
| 08/09/2026 | **v1.7** | fonctionnelles-haplan_specs_v1.7.md | Claude | **Texte libre** (§8.11/§9.3/§17.9) implémenté et déployé en conditions réelles sur les 3 sorties de HAPlan (web, carte Lovelace HA, écran ESP32) — élément positionné comme une icône mais sans `entity_id`, 3 paliers de taille, édition inline. Conception de départ ("page libre" = plan sans image, `filename` optionnel, cas spécial propagé dans 4 fichiers) **abandonnée en cours de route sur demande explicite de l'utilisateur** au profit d'une image unie générée UNE FOIS à la création (§4.3, portrait 4:3, fond transparent) — `filename` reste obligatoire, aucun cas spécial dans le code final. §17 (Lovelace) passe de "conception, aucun code" à **implémentée** — découverte que l'implémentation réelle avait en fait déjà été construite entre le 28/08 et le 08/09/2026 sans jamais être reflétée dans une mise à jour de spec (§17.5-17.8 réécrits : `card_mod` obligatoire en pratique, pas seulement natif comme supposé en v1.5). Bugs réels corrigés en testant : libellés de statut des interrupteurs génériques enfin corrects (bouton bascule unique remplacé par deux boutons explicites Éteindre/Allumer), nouveau type d'objet `input_boolean` (§9.4, auparavant reclassé à tort en chauffe-eau par collision de mot-clé), glisser-déposer cassé pour tous les objets/textes après un premier aller-retour en mode édition (référence DOM orpheline), dépôt Lovelace refusé malgré une seule vraie cible HA (doublons gossip du même hôte, côté `core`, `AppService.ts`). Ancienne version v1.6 archivée. |
| 27/08/2026 | **v2.8** | fonctionnelles-supervisor_specs_v2.8.md | Claude | **Visibilité multi-machines intra-site + page d'accueil** (§14bis) : registre d'applications par gossip MQTT (`AppGossipService`, même patron que `TargetGossipService`), entrées distantes affichées en lecture seule à côté du menu (pas de fusion), clic → redirection simple (pas de proxy HTTP/WebSocket — la vitrine unique reste assurée par la diffusion du même registre à chaque `core`). Nouveau lien HA local, nouvelle liste personnelle de sites externes jamais gossipée (`core.externalSites`), nouvelle page d'accueil par défaut. Vocabulaire "site" clarifié en session (propriété/foyer indépendant, aucune fusion de visibilité entre sites, WireGuard hors périmètre du dépôt). Deux bugs réels trouvés et corrigés en testant : sélection du module par défaut dupliquée dans `ModuleManager.ts` (écrasait "accueil"), `zigbee2mqttTargets` perdu silencieusement par 3 méthodes de `ConfigService` (même classe que l'incident `disabledApps` du 07/08/2026). Vérifié en conditions réelles (broker MQTT, navigateur). Étape 1 d'un besoin en deux temps — le besoin d'origine (application hors Docker) reste différé. Ancienne version v2.7 archivée. |
| 24/08/2026 | **v2.7** | fonctionnelles-supervisor_specs_v2.7.md | Claude | **Migration en process séparé de `ia`/`planificateur`/`haplan`/`arbreouquoi`** (§14.12) — reprend la décision différée en v2.6 (§14.10) : le "proxy de commandes HA" jugé nécessaire à l'époque n'existait pas à construire, `CorrelatedRequester` (déjà utilisé pour le dialogue ia↔planificateur, jusque-là dupliqué) suffisait, désormais centralisé dans le socle. Nouveaux `HaQueryBridge` (core, canal générique `ha:bridge:request`/`:reply`) et `HaBridgeClient` (façade côté app, seul type importé — jamais `HaStructureRegistry`/`HaWsClient` en direct), cache local synchrone pour les lectures fréquentes tenu à jour par les événements déjà existants (`ha:entity:state_changed`/`ha:ready`), `HaCommandService` retiré de `planificateur` (redondant). Les 12 applications du socle tournent désormais toutes en process séparé — désactiver n'importe laquelle depuis *Gestion des applications* n'entraîne plus de redémarrage de `core` (vérifié en conditions réelles : uptime ininterrompu pendant tout un cycle désactivation/réactivation de `haplan`). Vérifié en conditions réelles pour les 4 apps (arbre HA complet, résolution `obtenir_etat` via Mistral, planifications `state_change` actives, plan avec états en direct) — y compris une commande d'écriture réelle (`sendCommand`, extinction d'une lumière depuis HAPLAN) testée par l'utilisateur. Ancienne version v2.6 archivée. |
| 23/08/2026 | **v4.31** | techniques-socle-ha-mqtt_specs_v4.31.md | Claude | **Déploiement de dimotic-ha lui-même sur d'autres machines** (§4.3bis nouvelle) : nouveau `targets[]` sur le schéma config racine, nouveau `CoreDeployService.ts` (même socle SSH/SCP partagé que rpigpio/teleinfo/arexx, protocole `{targetId, action}` identique), remplace `docker/rebuild-and-deploy.sh` pour l'étape déploiement (build+push Docker Hub reste manuel) — copie `compose.deploy.yaml`, sème `data/core/config.yaml` (HA/MQTT réels, `disabledApps` = toutes les apps sauf core) uniquement s'il est absent. Nouvelle section IHM "Déploiement" (`DeploymentManager.ts`, réutilise `TargetCards.js`). Corollaire corrigé en cours de route : `ConfigService.saveConfig()`/`setDisabledApps()` ne préservaient pas `targets` — même classe de bug que l'incident `disabledApps` du 07/08/2026. Testé au navigateur (ajout/suppression réels, config réelle confirmée intacte) ; pas de test de déploiement contre ha2/orangepi (production). Ancienne version v4.30 archivée. |
| 23/08/2026 | **v1.4** | fonctionnelles-rpigpio_specs_v1.4.md | Claude | **Multi-cible standardisé** : `target` singulier → `targets[]` (`.max(1)`, id texte libre, même pattern que `teleinfo`/`arexx`). Deux simplifications décidées avec l'utilisateur, propagées au socle partagé : accès cible toujours en root direct (`sshUser` retiré, `sudo NOPASSWD` jugé équivalent à root) et clé SSH par cible sous `data/rpigpio/ssh/<id>/` au lieu de `~/.ssh/...` (non résolu dans le conteneur Docker). Nouvelle carte de cible dans le tableau de bord (`TargetCards.js`, composant mutualisé rpigpio/teleinfo/arexx) avec 4 boutons Déployer/Démarrer/Arrêter/Redémarrer réellement câblés. Testé au navigateur. Ancienne version v1.3 archivée. |
| 23/08/2026 | **v1.3** | fonctionnelles-teleinfo_specs_v1.3.md | Claude | **Multi-cible standardisé**, même refactor que rpigpio. **Migration réelle de `data/teleinfo/config.yaml`** (`target:` → `targets: [{ id: 'rpi1', ... }]`), vérifiée par reparsing. Testé au navigateur avec la vraie cible migrée. Ancienne version v1.2 archivée. |
| 23/08/2026 | **v1.5** | fonctionnelles-arexx_specs_v1.5.md | Claude | **Vrai pilotage SSH automatisé des émetteurs** : nouveau `ArexxDeployService.ts` (même patron que `teleinfo`), remplace la copie manuelle `scp`/`ssh` par des boutons Déployer/Démarrer/Arrêter/Redémarrer réels. Nouveau `targets[]` (AREXX = 1ère app à plusieurs cibles dès le départ, pas de `.max()`). Page Déploiement restructurée : carte adresse récepteur inchangée + nouvelle liste de cibles pilotables (`TargetCards.js`, composant mutualisé avec rpigpio/teleinfo). Testé au navigateur (rendu uniquement, pas de test réel contre un émetteur physique). Ancienne version v1.4 archivée. |
| 22/08/2026 | **v1.3** | fonctionnelles-rpigpio_specs_v1.3.md | Claude | **`DeployService.ts` migré sur le socle SSH/SCP partagé** `core/infrastructure/remote/` (§5.1bis), mutualisé avec `teleinfo`. Protocole Socket.io uniformisé : `rpigpio:deploy`/`rpigpio:deploy:result` → `rpigpio:remote-op`/`rpigpio:remote-op:result` (`{ action }`), même mécanisme quelle que soit l'intervention distante — demande explicite de l'utilisateur en prévision de futurs scripts start/stop/restart. `start()`/`stop()`/`restart()` ajoutés côté `DeployService` (délèguent à un `DockerContainerController` partagé), pas encore exposés en IHM. Ancienne version v1.2 archivée. |
| 22/08/2026 | **v1.2** | fonctionnelles-teleinfo_specs_v1.2.md | Claude | **`DeployService.ts` migré sur le socle SSH/SCP partagé** `core/infrastructure/remote/` (§7.1bis), mutualisé avec `rpigpio`. Protocole Socket.io uniformisé : `teleinfo:deploy`/`teleinfo:deploy:result` → `teleinfo:remote-op`/`teleinfo:remote-op:result` (`{ action }`). `start()`/`stop()`/`restart()` ajoutés côté `DeployService` (délèguent à un `SystemdUnitController` partagé), pas encore exposés en IHM. Ancienne version v1.1 archivée. |
| 22/08/2026 | **v1.4** | fonctionnelles-arexx_specs_v1.4.md | Claude | **Étape "Préparer l'accès SSH"** sur la page Déploiement (§9.2bis), entre le formulaire `target.txt` et la copie du dossier `drivers/` : affiche `ssh-keygen`/`ssh-copy-id` à lancer par l'utilisateur (`ssh-copy-id` non scriptable, exige un terminal interactif pour le mot de passe — décision explicite de ne pas automatiser côté serveur). Nouveau champ `ArexxStatus.isRunningInDocker` (§8.1/§9.3), alimenté par un nouveau mécanisme partagé du socle (`core/infrastructure/runtime/docker.ts` — détection `/.dockerenv` au bootstrap de `core`, transmise aux process enfants séparés via héritage `process.env`, même mécanisme déjà utilisé pour `PROJECT_ROOT`) : si vrai, un bloc conditionnel ajoute l'instruction d'ouvrir un terminal **dans** le conteneur (`docker exec -it`) avant de générer la clé. Testé au navigateur (bascule vérifiée dans les deux sens). Sans changement du mécanisme de déploiement lui-même (§5.4). Ancienne version v1.3 archivée. |
| 22/08/2026 | **v1.3** | fonctionnelles-arexx_specs_v1.3.md | Claude | **Formulaire réel sur la page Déploiement** (§9.2bis) : remplace l'édition manuelle de `data/arexx/drivers/target.txt` par deux champs (adresse IP, port) + bouton "Enregistrer", écrivant le fichier côté serveur (`arexx:driver-target:get`/`:save`, §9.3) après validation. `arexx:error` obtient son premier émetteur réel (jusqu'ici déclaré mais jamais utilisé depuis la v1.0). Testé au navigateur : cas succès (écriture confirmée sur disque) et cas erreur (adresse vide). Aucun changement du mécanisme de déploiement lui-même (§5.4). Ancienne version v1.2 archivée. |
| 21/08/2026 | **v1.2** | fonctionnelles-arexx_specs_v1.2.md | Claude | **Déploiement scripté sur une machine distante** (§5.4, nouveau) : `DriversBundle.ts` génère `data/arexx/drivers/` à chaque démarrage (copie depuis `applications/arexx/`, figé dans l'image Docker et donc inaccessible depuis l'hôte — `data/` est le volume monté) — contient `scripts/deploy-sender.sh` (détection d'architecture 32/64 bits, installation d'un service systemd, idempotent, arrête un service existant avant d'écraser ses fichiers), un second bundle vendored `tl-500/` (source C portable, base open-source `mochad`, alternative à `rf_usb_http.elf` sur les architectures où ce dernier ne fonctionne pas — arm64 notamment, limite déjà connue en interne mais jamais formalisée dans la spec avant cette version), et `target.txt` (adresse du récepteur lue par le script, remplace un argument en ligne de commande, préservé aux redémarrages). Validé en conditions réelles sur x86_64/ARM64/ARMv6 : mêmes capteurs, mêmes valeurs que la référence `rf_usb_http.elf` ; installation complète sur `bs510` avec ce mécanisme, chaîne bout en bout confirmée avec l'application `arexx` locale. RSSI de `tl-500` documenté comme non calibré (octet brut, pas un vrai dBm). Nouvelle page IHM "Déploiement" (§9.2bis, 4 étapes). Deux pièges de `rf_usb_http.elf` documentés pour la première fois (§5.3) : mode verbeux + rejeu d'historique interne du dongle = charge CPU/logs élevée (constaté en conditions réelles sur une machine de test), et la ligne `Z` (condition) de `rulefile.txt` doit être conservée malgré son statut "non supportée" dans le ReadMe officiel — la retirer fait planter le programme. Ancienne version v1.1 archivée. |
| 19/08/2026 | **v1.2** | fonctionnelles-rpigpio_specs_v1.2.md | Claude | **Déploiement réel sur stfort** (§1.4) : 3 pins réelles (relais/lumière, radiateur, journuit), `id` choisi manuellement (position physique du connecteur) pour permettre un calcul de topic sans table de correspondance. **Pont de compatibilité avec l'ancien système** (§2.5, nouveau) : redirige l'accès GPIO bas niveau de `zdidnodegpio` vers ce module, même principe que le pont RFXCOM (`INSTALLATION.md` partie B), non documenté en détail ici (hors périmètre du dépôt, code legacy sans gestion de version formelle). **Correction de topic réelle** (§4.1bis, nouveau) : segment `output` fixe côté mqtt-io, absent de `generator.ts` et non documenté avant cette version — trouvé et corrigé avant mise en service du pont. Comportement de démarrage de `initial` clarifié (§4.3) : mqtt-io lit et republie l'état existant au démarrage, n'écrit rien, par conception (`initial` volontairement non exposé par l'IHM). Caveat "non vérifié" de la présence d'agent (§7.3) levé. Application déployée sans moyen de se redéployer elle-même depuis stfort (décision explicite, pas de clé SSH stockée dans son propre volume de données). Ancienne version v1.1 archivée. |
| 18/08/2026 | **v1.2** | fonctionnelles-scriptsha_specs_v1.2.md | Claude | **Généralisation du provisionnement** (§4bis) : `ProvisioningConfig` structuré (4 champs) remplace le câblage en dur sur l'id du script minuterie — n'importe quel script diffusé portant ce champ bénéficie du même moteur générique (`reconcileEntityHelpers`). Nommage des helpers créés basé sur la taxonomie QUOI/OÙ déjà calculée par le core (même convention que `rpigpio::buildQuoiOuLabel`), avec anti-collision par suffixe numérique (la taxonomie ne garantit pas une unicité absolue). `matchesWatchCondition()` isolée dans sa propre méthode à la demande explicite de l'utilisateur, pour pouvoir évoluer rapidement sans toucher le reste du moteur. Revérifié en conditions réelles avec le nouveau nommage : 2 collisions taxonomiques réelles rencontrées et correctement désambiguïsées ("Chevets L1"/"L2"), idempotence confirmée. Ancienne version v1.1 archivée. |
| 18/08/2026 | **v1.1** | fonctionnelles-scriptsha_specs_v1.1.md | Claude | **Provisionnement automatique lumières↔timers** (§4bis) : au déploiement du script d'illustration "Ensemble de minuterie", création d'un helper `timer` HA pour chaque lumière (`light.*`) sans timer correspondant, et détection continue des nouvelles lumières (`ha:entity:updated`) tant que ce script reste diffusé. Ajoute côté core, génériques et réutilisables : `HaWsClient.listHelpers/createHelper/deleteHelper` (vraies commandes WebSocket `{domain}/create\|list\|delete`, vérifiées empiriquement) et `HaHelperBridge` (pont IPC générique par `appId`, même principe que `HaRestBridge`). Constat en creusant ce besoin : `ha:structure:get`/`ha:structure` (documentés dans `guide-nouvelle-application_specs`) sont eux aussi jamais implémentés — même écart que `InterAppClient`. Vérifié en conditions réelles (35 lumières réelles → 35 timers créés, idempotence confirmée en rejouant la diffusion, nettoyé après validation). Ancienne version v1.0 archivée. |
| 18/08/2026 | **v1.0** | fonctionnelles-scriptsha_specs_v1.0.md | Claude | **Nouvelle application — gestion de scripts Home Assistant** (dépôt de fichier YAML, titre/explication, diffusion/retrait à la demande vers `script.*`). Process séparé dès l'origine. Ajoute deux capacités génériques côté core, réutilisables par toute future app : `HaWsClient.getDomainConfig/setDomainConfig/deleteDomainConfig` (REST config HA, `HaAutomationBackupService` refactorée pour les réutiliser) et `HaRestBridge` (pont IPC générique routé par `appId`) ; route d'upload générique `POST /api/apps/:appId/upload` (généralise le précédent dédié HAPLAN, resté inchangé). Écart documenté au chapitre `InterAppClient` de `guide-nouvelle-application_specs` (§8 de la spec — confirmé absent du code réel partout ailleurs). |
| 16/08/2026 | **v2.6** | fonctionnelles-supervisor_specs_v2.6.md | Claude | **⭐ Phase 2 — migration terminée pour les 6 applications restantes prévues** (`rpigpio`, `teleinfo`, `arexx`, `evoo7`, `nommage`, `rfxcom`) et révision d'architecture en cours de route (§14.5-§14.11) : le pont EventBus↔app pivote de MQTT (`MqttEventBus`) vers IPC (`IpcEventBus`) — les apps migrées restent en pratique sur la même machine que `core`, qui les spawn lui-même, un tuyau `child_process` suffit, pas besoin d'un broker. Pont générique (3 mécanismes automatiques) éliminant le besoin de recopier `bridgedEvents` à la main pour chaque app. Présence/heartbeat des agents distants rpigpio/teleinfo implémentée (§11.5, teleinfo vérifiée en conditions réelles). Correctif du bug préexistant `app:menu:register` jamais relayé (trouvé en Phase 1, corrigé en Phase 2). Migration d'`ia`/`planificateur`/`haplan`/`arbreouquoi` explicitement différée (dépendance à des objets vivants `haWsClient`/`haStructureRegistry`). `MqttEventBus` non supprimé, juste plus utilisé par aucune app migrée aujourd'hui. Ancienne version v2.5 archivée. |
| 16/08/2026 | **v2.5** | fonctionnelles-supervisor_specs_v2.5.md | Claude | **⭐ Phase 1 implémentée et vérifiée en conditions réelles** (§14) : `espdisplay` migrée en process séparé (`MqttEventBus`, `ProcessSupervisor`, `SupervisorEventBridge`, `standalone.ts`). Deux problèmes réels trouvés en testant : process orphelins au redémarrage de `core` (corrigé), `app:menu:register` jamais relayé côté socle pour aucune app (préexistant, signalé dans TODO.md). Commandes MQTT start/stop/restart généralisées à toute app séparée, aucune signature sur ce canal pour cette phase (authentification prévue au niveau du broker mosquitto). Ancienne version v2.4 archivée. |
| 16/08/2026 | **v2.4** | fonctionnelles-supervisor_specs_v2.4.md | Claude | Correction de référence croisée uniquement (techniques-socle v4.29→v4.30). Ancienne version v2.3 archivée. |
| 16/08/2026 | **v4.30** | techniques-socle-ha-mqtt_specs_v4.30.md | Claude | **`parseIncomingCommand()` rejette les commandes MQTT retenues** (§8.5.4ter) — incident de sécurité réel RFXCOM : ~21 messages `.../set` retenus sur le broker de production, rejoués à chaque redémarrage et exécutés comme de vraies commandes RF433 dès que le transceiver était connecté — maison éteinte de façon imprévisible. Correctif socle, protège tous les modules (rfxcom, evoo7, arexx, nommage). Messages déjà présents nettoyés manuellement sur le broker. Ancienne version v4.29 archivée. |
| 16/08/2026 | **v5.16** | fonctionnelles-rfxcom_specs_v5.16.md | Claude | Nouvelle §17bis "Multi-instances — Recouvrement RF et Relais entre Bridges" : ferme une lacune documentaire (`registered-devices`, construit plus tôt cette session, jamais documenté ici, seulement dans `fonctionnelles-supervisor_specs` §9.4). Exclusion effective de la liste "découverts" pour un device revendiqué ailleurs (corrige un bug signalé par l'utilisateur), relais de valeur inter-instances (nouveau topic non retenu `relayed-value`) avec garde-fou anti-écho pour les commandes envoyées. Aucun relais de commande (hors scope explicite). Non vérifié en conditions réelles (nécessiterait deux dongles en recouvrement RF). Ancienne version v5.15 archivée. |
| 15/08/2026 | **v5.15** | fonctionnelles-rfxcom_specs_v5.15.md | Claude | Ferme le gap restant de v5.14 §20 : `lastValue`/`commandDeviceId` ajoutés à `rfxComDeviceSchema` (survivent désormais au rechargement). Fenêtre de fraîcheur de 30 min retirée de `publishDeviceStateAtStartup()` (demande utilisateur, devenue sans objet une fois `lastValue` persisté). Nouveau `lastAnyValueChangeAt` : horodatage global tous devices confondus, persisté, exposé dans `RfxComStatus` et sur le tableau de bord — sans logique de fraîcheur/alerte pour l'instant (demande explicite). Ancienne version v5.14 archivée. |
| 15/08/2026 | **v5.14** | fonctionnelles-rfxcom_specs_v5.14.md | Claude | **⚠️ Correctif de sécurité réel** (§9.2bis) : `publishReceiverStateAtStartup()` envoyait une vraie commande RF433 `turn_off` à tout récepteur commandable sans `lastOn` connu au démarrage — incident constaté par l'utilisateur (maison éteinte de façon imprévisible à certains redémarrages). Corrigé : republication strictement passive de l'état, plus aucune commande matérielle au démarrage. §20 corrigée (ligne "lastOn/lastLevel strippés" stale, déjà fixée le 07/08/2026 sans mise à jour du tableau). Ancienne version v5.13 archivée. |
| 15/08/2026 | **v2.3** | fonctionnelles-supervisor_specs_v2.3.md | Claude | Discussion approfondie, point par point, avant implémentation. §6.4 étendue : persistance locale horodatée du registre par machine (résilience si le broker perd son retain), horodatage source fait foi. §9 entièrement réécrite : principe "une entité, un endroit, responsabilité du paramétreur" ; `bridgeInstance` par défaut passe de "dérivé du machineId" à un tirage aléatoire persisté, généralisé à tous les modules à bridge ; `rpigpio` découvert sans aucun `bridgeInstance` (passe par mqtt-io externe, collision réelle dès aujourd'hui) ; `node_id`=`bridgeInstance` tranché pour le topic de découverte ; nouveau mécanisme RFXCOM de diffusion des devices enregistrés + avertissement UI pour le recouvrement RF réel. §10 réécrite : signature HMAC du canal de commandes (clé partagée), authentification broker/TLS explicitement écartée. §11 nouvelle : Agent Minimal pour Machines Contraintes (ARMv6/teleinfo) — présence + jeu fermé de commandes nommées, même principe que la commande forcée SSH d'espdisplay. §13.1 nouvelle : liste de 7 prérequis avant implémentation. Ancienne version v2.2 archivée. |
| 15/08/2026 | **v4.29** | techniques-socle-ha-mqtt_specs_v4.29.md | Claude | **Deux correctifs distincts**, tous deux issus de bugs réels constatés par l'utilisateur, tous deux en creusant "la navigation entre applications pose des problèmes". (1) `arbreouquoi` rejoint la convention `window.{id}App.init()` (§6.1) — laissé de côté par le correctif v4.28, symptôme identique (écran figé après revisite) désormais corrigé, avec scission écouteurs socket (une fois)/écouteurs DOM (à chaque revisite). (2) Bug réel dans `Sidebar.ts` (§6.2) : un module sans `menu.pages` (ex: ESPDISPLAY, HAPLAN) était rendu avec `href="#moduleId"` au lieu de `entry.path`, alors que l'attachement de l'écouteur de clic cherchait ce dernier — clic strictement sans effet. Corrigé : `entry.path` utilisé comme `href` dès qu'il existe, indépendamment de la présence de `pages`. Ancienne version v4.28 archivée. |
| 15/08/2026 | **v1.8** | fonctionnelles-planificateur_specs_v1.8.md | Claude | Retrait de l'entrée "Macros" du sous-menu Paramètres Techniques (§11) — redondante avec l'accès déjà possible depuis l'application elle-même (bouton "📋 Macros" dans `index.html`, vers `planificateur/config.html`). §11 corrigée (claim périmé "macros + planifications, mêmes actions"). Ancienne version v1.7 archivée. |
| 15/08/2026 | **v2.2** | fonctionnelles-supervisor_specs_v2.2.md | Claude | Politique de redémarrage en cas de crash (§8.4) : backoff exponentiel plafonné à 30s, réarmement du compteur après 60s de fonctionnement stable, abandon après 5 tentatives rapprochées (état terminal `crashed`, redémarrage manuel requis, remonté dans le registre/UI). Discutée puis validée avec l'utilisateur avant rédaction. Ancienne version v2.1 archivée. |
| 15/08/2026 | **v2.1** | fonctionnelles-supervisor_specs_v2.1.md | Claude | §7 réécrite (réponse directe à "voir toutes les applis de toutes les machines sur la même interface web ?") : corrige une incohérence de la v2.0 (pontage Socket.io limité à sa propre machine, alors que le bus MQTT était déjà global par construction, §7.1) et ajoute le proxy HTTP de repli pour les fichiers statiques d'une application distante (§7.2 — seule pièce manquante, le service de fichiers ne passant jamais par le process de l'application). Registre étendu (`address`/`webPort`). Ancienne version v2.0 archivée. |
| 15/08/2026 | **v2.0** | fonctionnelles-supervisor_specs_v2.0.md | Claude | **Refonte complète, même jour que la v1.0** — aucun code écrit. Passage à une application = un process OS (au lieu d'un seul process partagé), MQTT comme bus unifié intra **et** inter-machine (la v1.0 ne l'utilisait que pour le cross-machine), activation/désactivation d'application en direct par signal (résout enfin le redémarrage complet actuel, explicitement hors scope en v1.0), interface web unique explicitement préservée (Express/Socket.io restent dans `core`, seule la source du pontage dynamique change de l'EventBus vers MQTT). Motivé par une mesure réelle sur `ha2` (`docker top`) écartant la crainte de surcoût mémoire par process : l'architecture actuelle fait déjà tourner 4 process pour un seul "logique" (tsx/esbuild), ~332 Mo au total. Ancienne version v1.0 archivée. |
| 15/08/2026 | **v1.1** | fonctionnelles-espdisplay_specs_v1.1.md | Claude | **Exécution distante par SSH** (§6.3) : corrige un échec réel en production (bouton HAPLAN "Déployer sur l'écran" utilisé depuis `ha2`, qui n'a ni python3 ni le conteneur esphome — ENOENT). Clé SSH dédiée, commande forcée côté cible, `UserKnownHostsFile` persisté, `openssh-client` ajouté à l'image Docker (3 bugs distincts trouvés en conditions réelles). Gagne aussi une configuration UI (§5.2, menu "Écrans ESP", absente en v1.0). Ancienne version v1.0 archivée. |
| 15/08/2026 | **v5.13** | fonctionnelles-rfxcom_specs_v5.13.md | Claude | La page application affiche désormais le statut matériel du transceiver (§12.2/§12.3 : type de récepteur, firmware, protocoles activés/disponibles) — déjà récupéré en interne (§8.2, filtre matériel des protocoles) mais jamais exposé à l'UI. Vérifié en conditions réelles (RFXtrx433 XL, firmware ProXL 2 v1047). Ancienne version v5.12 archivée. |
| 28/08/2026 | **v1.6** | fonctionnelles-haplan_specs_v1.6.md | Claude | **Mécanisme de dépôt révisé** (§17.5/§17.8) : dépôt de fichier par SSH plutôt que le WebSocket envisagé en v1.4/v1.5 — `lovelace/config/save` non documentée officiellement par HA, jugée trop fragile. Instance réelle de l'utilisateur vérifiée en direct (`ha2`) : tableaux de bord en mode storage, écriture directe risquée (HA peut écraser). Solution : nouveau tableau de bord dédié en mode YAML (coexiste avec l'existant, jamais de conflit), déclaré une fois dans `configuration.yaml` côté HA (redémarrage HA ponctuel, pas de code). Écriture par le mécanisme SSH déjà éprouvé pour la config HA/Mosquitto (`SshClient.ts`/`HaStackDeployService.ts`), image de fond vers `config/www/` (`/local/...`). Conception toujours non implémentée — mécanisme confirmé, pas encore testé en conditions réelles. Ancienne version v1.5 archivée. |
| 28/08/2026 | **v1.5** | fonctionnelles-haplan_specs_v1.5.md | Claude | **Réponse aux deux points ouverts de la v1.4** (§17.2/§17.7) : police d'icônes HAPlan identifiée en lisant le code (Font Awesome 6 Free Solid, CDN, ~8 glyphes utilisés au total, déjà partagés avec la génération ESPHome — table de correspondance FA→mdi manuelle à construire, petite et finie) ; capacités de la carte Plan Lovelace HA confirmées via sa documentation officielle (`picture-elements`/`state-icon`) — coordonnées en % déjà compatibles avec la convention HAPlan, couleur dynamique par état native (`state_color`), fenêtre thermostat déjà couverte nativement par le dialogue `more-info` standard (rend probablement inutile le repli +/- envisagé en v1.4). Conception toujours non implémentée — capacités confirmées par la documentation, pas encore vérifiées en conditions réelles. Ancienne version v1.4 archivée. |
| 28/08/2026 | **v1.4** | fonctionnelles-haplan_specs_v1.4.md | Claude | **Génération de cartes Plan Home Assistant (Lovelace)** (§17, nouvelle, conception soumise par l'utilisateur, aucun code écrit) : troisième sortie de HAPlan depuis la même modélisation plans/positions (§4), après l'interaction HA directe et la génération ESPHome (§3.6/§8.9-8.10) — carte Plan Lovelace construite directement au format cible HA, envoyée en WebSocket (port 8123, pas MQTT) via le token longue durée déjà géré côté cœur, déclenchement manuel. Types d'objets génériques (actionneur simple, capteur pur, objet complexe avec fenêtre dédiée ou repli +/− pour les thermostats). Points laissés ouverts avant implémentation (§17.7) : police d'icônes réelle de HAPlan à identifier, capacités exactes de la carte Plan Lovelace côté HA non vérifiées. Ancienne version v1.3 archivée. |
| 15/08/2026 | **v1.3** | fonctionnelles-haplan_specs_v1.3.md | Claude | Nouvelle §8.10 "Écran mural physique (ESP32-S3)" : veille du rétroéclairage après 30s d'inactivité tactile (rallumé au premier tap), retrait du texte des coordonnées x/y (le marqueur "X" rouge reste, décision utilisateur permanente). Ancienne version v1.2 archivée. |
| 15/08/2026 | **v1.4** | fonctionnelles-arbreouquoi_specs_v1.4.md | Claude | Corrige §6.3 : le libellé du QUOI est maintenant affiché à côté de l'icône dans tous les cas, y compris quand l'icône est reconnue — la v1.3 ne l'affichait qu'en repli pour les icônes inconnues (`❓`), contrairement à la demande initiale du 08/08/2026. Ancienne version v1.3 archivée. |
| 15/08/2026 | **v1.0** | fonctionnelles-supervisor_specs_v1.0.md | Claude | **Nouvelle spécification — conception validée, aucun code écrit** : système de supervision multi-machines décentralisé (pas de machine centrale), motivé par deux problèmes concrets de la session (SSH point-à-point d'`espdisplay` vers falbala, risque de collision `bridgeInstance` entre deux machines faisant tourner `rpigpio`/`rfxcom`). Identité machine (`core.machineId`), présence + registre d'applications par MQTT (topics `dimotic/supervisor/{machineId}/status\|apps`, mêmes conventions LWT/retain que l'existant), relais générique de commandes inter-machines (`emitToMachine()`, cas cible : migration d'espdisplay hors SSH), traitement explicite des deux classes de redondance rpigpio/rfxcom, et constat de sécurité sur l'absence d'authentification des deux brokers MQTT observés en conditions réelles (ha2, stfort). Implémentation différée à une session ultérieure. |
| 13/08/2026 | **v1.2** | fonctionnelles-haplan_specs_v1.2.md | Claude | **Bouton "Déployer sur l'écran"** (§3.6, §8.9) : premier exemple de communication inter-applications initiée par HAPLAN, vers la nouvelle application `applications/espdisplay` (`espdisplay:deploy-floorplan`/`espdisplay:deploy-result` sur l'EventBus générique, même pattern que `integration:bridge:register`, aucune dépendance de compilation entre les deux applications). 3 nouveaux événements Socket.io (§13, tous ponctuels), 2 nouveaux codes d'erreur (`HAPLAN_DEPLOY_BUSY`/`HAPLAN_DEPLOY_FAILED`, §3.5). Testé en conditions quasi réelles : EventBus simulé de bout en bout avec les deux vrais services + vrai sous-processus + vraie compilation Docker. Ancienne version v1.1 archivée. |
| 13/08/2026 | **v1.0** | fonctionnelles-espdisplay_specs_v1.0.md | Claude | **Nouvelle application ESPDISPLAY** : orchestration du déploiement de firmware sur les écrans ESP (ESPHome/LVGL), découplée de HAPLAN par communication EventBus générique — HAPLAN reste propriétaire du pipeline Python de génération (`generate_esphome_floorplan.py`, enrichi cette même session d'options `--merge`/`--compile` stables, remplaçant des heredocs tapés à la main pendant le débogage de l'écran physique). Choix d'hébergement Docker documenté avec l'essai réel sur `ha2` (Pi4, 1.8 Go RAM) : swap saturé dès le téléchargement du toolchain ESP-IDF, machine de production HA devenue injoignable en SSH, redémarrage électrique nécessaire — conteneur `esphome` conservé sur la machine de développement. Aucune conclusion positive établie pour un Pi5 (`ia.local`), essai commencé puis abandonné avant compilation réelle. |
| 12/08/2026 | **v1.9** | fonctionnelles-ia_specs_v1.9.md | Claude | **Comparatif multi-modèles** (§14, nouveau) : jusqu'à 4 modèles (Mistral Small/Medium, Claude Haiku/Sonnet via la couche de compatibilité OpenAI d'Anthropic) interrogés en parallèle sur la même phrase, toujours en dry-run strict — jamais de transmission à `planificateur`, quel que soit le fournisseur actif. **Vérification post-décision des références quoi/lieux/entity_id** (§8.2, nouveau `referenceValidator.ts`) : tout JSON structuré, plus les étapes de `DeployResponder` (§10) — "d'où qu'il vienne", demande utilisateur explicite — vérifié contre `HaStructureRegistry` avant transmission ; référence invalide → relance forcée puis refus explicite avec demande de correction si le problème persiste. Deux bugs trouvés et corrigés en creusant : la vérification ne couvrait initialement pas `executer_action` en dry-run (toujours "succès" sans vérifier) ; `tool_choice="any"` (convention Mistral) traduit en `'required'` pour Anthropic (400 sinon). **Étude comparative du 12/08/2026 sur 30 cas** (§14.6, commandes et résultats détaillés) : Mistral Small confirmé comme fournisseur actif — meilleur rapport latence (~4,6s vs ~9-16s)/coût (0,97€ vs 6$ sur ~3 jours)/fiabilité pour l'usage domotique réel. Toutes demandes utilisateur, session du 11-12/08/2026. Ancienne version v1.8 archivée. |
| 11/08/2026 | **v4.28** | techniques-socle-ha-mqtt_specs_v4.28.md | Claude | **Deux correctifs distincts, tous deux issus de bugs réels constatés par l'utilisateur** : (1) `ModuleContainer` rappelle désormais `window.{moduleId}App.init()` après réaffichage d'un module depuis son cache (§6.1) — corrige des dashboards inertes après revisite depuis le menu latéral (formulaire "Tester une commande" de IA sans effet, plus aucune requête n'atteignant `ia:test:send` ; écran Planificateur bloqué sur "Chargement") : le HTML réinjecté par `innerHTML` ne rebranchait aucun écouteur pour les apps vanilla TS, `Alpine.initTree()` étant sans effet pour elles. `init()` rendu idempotent côté `setupEventListeners()` (nouveau drapeau `listenersReady`) dans `ia`/`planificateur`/`evoo7`/`arexx`/`rfxcom`/`nommage` — `arbreouquoi` non touché, n'utilise pas cette convention, hors périmètre. (2) **`HaStructureRegistry.getLieuCatalog()`** (§8.3.3 nouvelle) — catalogue de lieux statique (tous niveaux confondus, ~65 valeurs distinctes constatées), complément de `getQuoiCatalog()` déjà existant, exposé côté `ia` pour réduire les faux refus "quoi_introuvable" en fournissant à Mistral une vérité de terrain stable, propice au cache de prompt. Session du 11/08/2026. Ancienne version v4.27 archivée. |
| 11/08/2026 | **v1.8** | fonctionnelles-ia_specs_v1.8.md | Claude | **Deux correctifs anti-hallucination contre les faux refus "quoi_introuvable"** constatés en direct ("allume la salle", une area réelle à 4 lumières, refusée à tort par Mistral). **Catalogue quoi/lieux statique injecté dans le prompt système** (§5, nouveau — `RulesProvider` accepte un `HaStructureRegistry` optionnel en 3ème paramètre, `getQuoiCatalog()`/`getLieuCatalog()` ajoutés à la suite de `regles_mistral.txt` à chaque appel, voir `techniques-socle-ha-mqtt_specs` v4.28 §8.3.3) — effet mesuré en direct : "allume la salle" se résout dès le premier round, sans relance forcée. **Relance forcée `tool_choice=any`** (§8.1, nouveau) — filet de sécurité résiduel : `MistralClient.streamChat()` accepte un `toolChoice` optionnel (jamais par défaut sur tous les rounds, empêcherait d'atteindre le round final texte/JSON de planification/gestion/macro) ; `IaService` détecte `"quoi_introuvable"` répondu sans qu'aucun outil n'ait été appelé dans tout l'échange (`isUnverifiedQuoiIntrouvable()`) et relance une seule fois ce round en forçant un outil (`quoiIntrouvableRetried`, jamais deux fois de suite). Testé en direct 3/3, aucune régression sur action directe/création de planification. Session du 11/08/2026. Ancienne version v1.7 archivée. |
| 11/08/2026 | **v4.27** | techniques-socle-ha-mqtt_specs_v4.27.md | Claude | **Graphe de lieux centralisé dans `HaStructureRegistry`** (§8.3.2 nouvelle, `getEntitiesByQuoiAndLieux()`) — remplace la résolution `quoi`/`lieux` dupliquée et divergente entre `planificateur/resolution.ts` et `ia/ToolExecutor.ts`. Résolution indépendante du niveau taxonomique (area/`lieu_pere`/`lieu_grand_pere`/`lieu_precis`) via un graphe de containment construit paresseusement, plus un repli tokenisé pour les phrases composées ("plafonnier de la chambre"). Bug réel corrigé en cours de route : `lieu_precis` exclu du graphe de containment (des labels comme "plafonnier" sont réutilisés par de nombreuses pièces sans rapport, les y inclure faisait converger toutes ces pièces vers le même nœud — constaté en testant "éteins les toilettes du haut" en direct, qui éteignait aussi cuisine/chambre/bureau). Toutes demandes utilisateur, session du 10-11/08/2026. Ancienne version v4.26 archivée. |
| 11/08/2026 | **v1.7** | fonctionnelles-planificateur_specs_v1.7.md | Claude | **Écran principal en liste numérotée** (§11) : indicateur actif/inactif, nom facultatif, phrase, prochaine exécution ; journaux existants déplacés en onglet séparé ; boîte de dialogue de création (phrase soumise à `ia` pour validation). **Identifiant numérique stable** `id` (§2), résolution par numéro en plus du nom pour les opérations de gestion (§4). **Résolution de `lieux` déléguée au graphe de lieux centralisé** de `HaStructureRegistry` (§7) — généralise le repli `lieu_precis` v1.6 aux phrases composées et aux lieux qualifiés par étage, `entities_snapshot` enrichi de `lieu_pere` (§6/§7). **Mise à jour live des dashboards** après une planification créée/gérée par conversation (§11). Toutes demandes utilisateur, session du 10-11/08/2026. Ancienne version v1.6 archivée. |
| 11/08/2026 | **v1.7** | fonctionnelles-ia_specs_v1.7.md | Claude | **`lister_entites`/`obtenir_etat` délégués au graphe de lieux centralisé** (§7, `HaStructureRegistry.getEntitiesByQuoiAndLieux()`) — remplace une résolution propre à `ToolExecutor.ts` limitée aux areas HA, sans repli, jamais documentée comme telle. `regles_mistral.txt` enrichi (`lieu_pere`, résolution numéro→planification pour la gestion). Toutes demandes utilisateur, session du 10-11/08/2026. Ancienne version v1.6 archivée. |
| 10/08/2026 | **v1.6** | fonctionnelles-ia_specs_v1.6.md | Claude | **Throttling préventif + backoff réactif sur rate limit Mistral** (§3, nouveau module `RateLimiter.ts` par modèle — mistral-small 1,67 req/s/100k tokens/min, mistral-large 0,25 req/s/400k tokens/min), **tokens consommés affichés par appel** (§3/§13), **trace complète de la réponse planificateur** dans le journal des échanges (§13), **historique des commandes de test** (`<datalist>`, §13). Demandes utilisateur, rate limit constaté en direct pendant les tests. Ancienne version v1.5 archivée. |
| 10/08/2026 | **v1.6** | fonctionnelles-planificateur_specs_v1.6.md | Claude | **Résolution de "lieux" sur le lieu précis en repli de l'area HA** (§7, corrige "éteins le salon" ciblant toute la salle — "Salon" n'existe qu'en attribut d'entité `attributs_taxonomie.lieu_precis`, jamais comme area HA), `entities_snapshot` enrichi de `lieu_precis` (§6). **Deux journaux sur le tableau de bord** (§8) : actions reçues de `ia`, détail des commandes envoyées à HA (issue réelle resolved/fallback/ignored, entité déclenchante, prochaine exécution) — l'ACK générique HA masquait les échecs RFXCOM réels jusqu'à Mistral, qui pouvait confabuler un succès. Ancienne version v1.5 archivée. |
| 10/08/2026 | **v5.12** | fonctionnelles-rfxcom_specs_v5.12.md | Claude | **Trois correctifs suite à une anomalie constatée en direct** (transceiver débranché, commande "réussie" sans transmission RF, HA affichant un état faux) : reconnexion automatique du transceiver (§8.6, boucle 5s), publication optimiste d'état corrigée (§8.7, vérification `isConnected()` avant tout envoi), journal des ordres reçus avec résultat d'exécution réel (§8.8, `rfxcom:orders:list`, 100 max). Ancienne version v5.11 archivée. |
| 10/08/2026 | **v1.5** | implementation-rfxcom_specs_v1.5.md | Claude | Miroir technique de la v5.12 fonctionnelle : `isConnected()` + `recordOrder()` (§6.1), boucle de reconnexion automatique (§11.1). Ancienne version v1.4 archivée. |
| 10/08/2026 | **v4.26** | techniques-socle-ha-mqtt_specs_v4.26.md | Claude | **Trois correctifs de l'affectation automatique des areas HA** (§8.5.4/§8.5.4bis nouvelle), trouvés en creusant un bug remonté par l'utilisateur ("à chaque redémarrage il y a plein de zigbee mal pris en compte, pas forcément les mêmes") : (1) nouveau second déclencheur de republication de découverte sur le birth message natif de HA (`homeassistant/status`, `HA_STATUS_TOPIC`), indépendant de notre propre connexion MQTT — événement générique `integration:{module}:ha:online`, consommé par RFXCOM et NOMMAGE ; (2) `HaStructureRegistry` : retrait d'un nettoyage d'areas vides qui supprimait à tort des areas fraîchement créées avant leur première entité, causant des créations WS en double rejetées par HA ("already in use") ; (3) `AreaEnsureService.registryReady` (verrou "référentiel prêt") réarmé sur `ha:disconnected` — ne se réarmait jamais après une reconnexion WS, alors que le référentiel est intégralement rechargé à chaque reconnexion, causant 191 échecs "already in use" sur une seule reconnexion avant ce correctif. Vérifié en conditions réelles (redémarrage complet HA + vidage du registre) : 127 devices, 0 device physique sans pièce. Ancienne version v4.25 archivée. |
| 10/08/2026 | **v1.6** | fonctionnelles-nommage_specs_v1.6.md | Claude | **Deux correctifs** liés au bug ci-dessus : ordre de priorité du champ source corrigé (§4.1) — `device.name` passe en premier (portait déjà la convention `QUOI---LIEU`, contrairement au nom d'une entité individuelle qui masquait ce champ dans l'ancien ordre) ; nouvelle §7.5, republication sur le signal `ha:online` du socle. Vérifié en conditions réelles : 0 device physique sans pièce, contre 41 avant ces deux correctifs. Ancienne version v1.5 archivée. |
| 10/08/2026 | **v1.6** | implementation-nommage_specs_v1.6.md | Claude | Miroir technique de la v1.6 fonctionnelle : exemple de code `processDiscoveryMessage()` réordonné (§4.1), nouvelle §4.2.3 (listener `integration:nommage:ha:online`, réutilise `reloadConfigAndReconnectMqtt()`). Ancienne version v1.5 archivée. |
| 10/08/2026 | **v5.11** | fonctionnelles-rfxcom_specs_v5.11.md | Claude | **Second déclencheur de republication de découverte** (§17.1) — `integration:rfxcom:ha:online`, alimenté par le birth message MQTT natif de HA, en plus du démarrage/de la reconnexion du bridge propre à RFXCOM. Ancienne version v5.10 archivée. |
| 10/08/2026 | **v1.4** | implementation-rfxcom_specs_v1.4.md | Claude | Miroir technique de la v5.11 fonctionnelle : nouveau listener `integration:rfxcom:ha:online` documenté (§11.1), rappelle `publishInitialDiscoveries()`. Ancienne version v1.3 archivée. |
| 09/08/2026 | **v4.25** | techniques-socle-ha-mqtt_specs_v4.25.md | Claude | **`name` nullable + `has_entity_name` systématique** (§8.5.4) — `EssentialEntityData.name`/`HaMqttDiscoveryEntity.name` acceptent `string \| null`, `buildDiscoveryPayload()` positionne `has_entity_name: true` sur toute entité. Corrige un doublon réel observé en direct sur une instance HA ("Lumière lumière", "Température Température") : des modules passaient le même mot dans `name` et `device.name`, HA (discovery classique) concatène toujours sans dédupliquer. Voir `fonctionnelles-rfxcom_specs` v5.10 pour les 5 endroits concernés. Ancienne version v4.24 archivée. |
| 09/08/2026 | **v5.10** | fonctionnelles-rfxcom_specs_v5.10.md | Claude | **§5.5 : `essential.name` passe à `null`** dans les 5 endroits concernés (ReceiverLight/Switch/Cover, RfxComService sensor/meter + scène) — corrige le doublon de nom ci-dessus. Ancienne version v5.9 archivée. |
| 09/08/2026 | **v1.5** | fonctionnelles-nommage_specs_v1.5.md | Claude | **Traduction des noms d'entité par pays** (§3.7, nouveau ; §3.4 étape 1bis ; §4.3) — `TranslationsRepository`, dictionnaires YAML `object_id -> libellé` (un par pays : seeds versionnés dans `src/defaultconfig/`, copie runtime dans `data/nommage/translations/` jamais écrasée), nouveau paramètre `nommage.language.country` (défaut `"France"`). Pays sans fichier connu généré à la volée (clés de France, valeurs d'English, mécanisme de mise à jour ultérieure volontairement pas défini). Remplace un ancien dictionnaire codé en dur (jamais documenté) puis une surcharge ponctuelle côté zigbee2mqtt (elle non plus jamais documentée), demande utilisateur explicite pour n'avoir qu'un seul endroit à maintenir. Ancienne version v1.4 archivée. |
| 09/08/2026 | **v1.5** | implementation-nommage_specs_v1.5.md | Claude | **`TranslationsRepository`** (§3 arborescence, §4.2 exemple `emitPassthroughDiscovery` mis à jour, §6.3 config YAML) — détail technique du correctif ci-dessus. Ancienne version v1.4 archivée. |
| 09/08/2026 | **v1.3** | fonctionnelles-arbreouquoi_specs_v1.3.md | Claude | **Hiérarchie OÙ du panneau de détails corrigée** (§6.2 — ordre et libellés de niveau, panneau réellement visible dans une boîte) ; **Légende OÙ avant Légende QUOI, types QUOI sans icône nommés, ID technique retiré de la liste d'entités** (§6.3). Document par ailleurs resté daté sur les mockups (hiérarchie "Area" à un seul niveau) — non corrigé, hors-scope de cette session. Ancienne version v1.2 archivée. |
| 09/08/2026 | **v1.3** | implementation-arbreouquoi_specs_v1.3.md | Claude | **`extractOuSegments()`** (§6.2bis, nouveau) — remplace le calcul de niveau OÙ par position (`getOuLevel`), devenu ambigu (pas seulement inversé) depuis que `lieu_precis` peut être `null`. **Fix CSS Shadow DOM `:root` → `:root, :host`** (§3.8) — cassait silencieusement toute la thématisation par variable CSS de l'application (pas seulement les symptômes rapportés : panneau de détails sans fond/ombre, décalage OÙ invisible). Changement de forme `ArbreOuQuoiEntityDetailsPayload.ouPath` (§3.5). Ancienne version v1.2 archivée. |
| 07/08/2026 | **v4.24** | techniques-socle-ha-mqtt_specs_v4.24.md | Claude | **Suppression du volume Docker nommé `app-code`** (§11.2 réécrite) — sa seule raison d'être (faire fonctionner `fs.renameSync()` entre `applications/`/`applications_désactivées/` sous overlay2) a disparu suite au remplacement du mécanisme d'activation/désactivation d'application par une liste `disabledApps` dans `data/core/config.yaml` (question utilisateur : *"si au lieu de déplacer, était géré dans le fichier config une liste des applications disable..."*). `/app` vit désormais dans les couches de l'image + la couche conteneur éphémère habituelle, comme n'importe quelle image sans état. §11.3/§11.4 mis à jour, `docker/deploy-remote.sh` grandement simplifié (pull + up -d suffit, plus de recréation de volume). Vérifié en local et en direct sur `ha2` (image `0.1.7`). Ancienne version v4.23 archivée. |
| 04/08/2026 | **v4.23** | techniques-socle-ha-mqtt_specs_v4.23.md | Claude | **Rattrapage de dérive code/specs** (session de vérification demandée par l'utilisateur, ~24h après la v4.22, suite à un audit constatant que 3 des 5 derniers correctifs fonctionnels n'avaient ni entrée `TODO.md` ni mise à jour de spec) : (1) **§5.3/§5.4.3** — `SOCLE_SOCKET_EVENTS` scindé en deux constantes (serveur→client / client→serveur, `SOCLE_CLIENT_EVENTS` nouveau) après découverte d'un bug de double-traitement des 8 événements client→serveur du socle (`config:get/save/validate`, `logs:get`, `ha:structure:get`, `ha:command:send`, `app:modules:config:get/save`) — `registerCoreSocketEvents()` transmettait l'objet complet à `SocketBridge.setupDynamicAppHandlers()`, qui câblait un second `socket.on(...)` pour des événements déjà gérés en dur ; repéré via une reconnexion MQTT en double sur NOMMAGE, vérifié en direct après correctif (un seul traitement par requête). (2) **§10.3/§10.4** — bouton de redémarrage manuel (Paramètres Techniques → Journalisation, `app:restart`). (3) **§11** — numéro de version réel de l'image Docker Hub (`X.Y.Z` incrémenté manuellement, `:0.1.3` au moment de cette version, sans lien avec `package.json`), commande `docker buildx` documentée. (4) **§11.4** — `compose.deploy.yaml` (fichier de déploiement autonome pour machine cible), avec avertissement sur la fiabilité des cartes SD suite à un incident réel constaté le jour même (Pi4 : `ENOENT` sur `/tmp` provoqué par une carte SD défaillante, crash-loop du conteneur). (5) **§7.5** — correctif d'un crash au démarrage sur champ YAML `null` (`deepMerge`/`loader.ts`), commité (`8e4217d`) sans trace ni `TODO.md` ni specs jusqu'ici. Ancienne version v4.22 archivée. |
| 04/08/2026 | **v1.4** | fonctionnelles-nommage_specs_v1.4.md | Claude | **Application à chaud d'une sauvegarde de configuration** (§7.4, nouveau) — la sauvegarde via le formulaire générique "Paramètres du Module" (seule UI de configuration de NOMMAGE) ne reconnectait pas les sources MQTT avec les nouveaux paramètres ; corrigé, vérifié en direct sur demande explicite de l'utilisateur. Ancienne version v1.3 archivée. |
| 04/08/2026 | **v1.4** | implementation-nommage_specs_v1.4.md | Claude | **Reconnexion MQTT sur sauvegarde via le formulaire générique** (§4.2.2, détail technique du correctif ci-dessus) — nouveau listener sur `app:module:config:saved` en plus de `nommage:config:save` (jamais déclenché par une UI réelle), méthode `reloadConfigAndReconnectMqtt()` factorisée. Mentionne le piège apparenté du double-traitement corrigé au niveau du socle (voir `techniques-socle-ha-mqtt_specs` v4.23 §5.4.3). Correction `ws-ha`→`dimotic-ha` (§7.1). Ancienne version v1.3 archivée. |
| 04/08/2026 | **v1.2** | fonctionnelles-arbreouquoi_specs_v1.2.md | Claude | **Correction de la référence `ws-ha`→`dimotic-ha`** (§1.1, projet renommé le 04/08/2026), sans changement fonctionnel. Ancienne version v1.1 archivée. |
| 04/08/2026 | **v1.2** | implementation-arbreouquoi_specs_v1.2.md | Claude | **Correction de la référence `ws-ha`→`dimotic-ha`** (§9.2), sans changement fonctionnel. Ancienne version v1.1 archivée. |
| 04/08/2026 | **v1.1** | fonctionnelles-arexx_specs_v1.1.md | Claude | **Correction de la référence `ws-ha`→`dimotic-ha`** (§5.1), sans changement fonctionnel. Ancienne version v1.0 archivée. |
| 04/08/2026 | **v1.9** | guide-nouvelle-application_specs_v1.9.md | Claude | **Correction de la référence `ws-ha`→`dimotic-ha`** (§3.1), sans changement fonctionnel. Ancienne version v1.8 archivée. |
| 03/08/2026 | **v1.2** | fonctionnelles-evoo7_specs_v1.2.md | Claude | **Rattrapage de dérive code/specs** : taxonomie en 5 champs séparés (§3.3, remplace le parsing jamais fonctionnel de `description`), paramétrage consolidé dans le formulaire générique avec rechargement à chaud désormais fonctionnel (§4, contredit une limitation documentée en v1.1), classification étendue — `binary_sensor` forcé, `deviceClass`/`unitOfMeasurement`, `entity_category` réellement `diagnostic` pour les données en lecture seule (§5). **Découverte majeure** : entité `climate` composite (température/consigne/mode/action sur 7 données EVOO7 liées) **entièrement implémentée** (§9, nouvelle section) — `TODO.md` la référence encore par erreur comme "non commencée", à corriger séparément. Topic MQTT dédié aux attributs de taxonomie et retrait de découverte (§10, mécanisme générique du socle). Précision : `$date$` toujours non substitué mais concerne 43/43 formats (pas 41/43 comme précédemment estimé). Ancienne version v1.1 archivée. |
| 03/08/2026 | **v4.22** | techniques-socle-ha-mqtt_specs_v4.22.md | Claude | **Deuxième réécriture de la §11 "Docker"**, quelques heures après la v4.21 — la conception "code monté depuis l'hôte" obligeait à `git clone` le dépôt sur chaque machine cible avant de démarrer le conteneur, à l'encontre de l'intérêt même de publier sur Docker Hub (question utilisateur : "pourquoi les applications seraient déplacer en externe du docker pourquoi pas en interne ?"). Nouvelle conception autosuffisante (`docker pull` + `docker compose up`, sans clone git) : le code est désormais construit pendant `docker build` (plus de service `build` séparé). Piège réel découvert et documenté en détail en vérifiant ce nouveau design : `fs.renameSync()` (mécanisme d'activation/désactivation d'application) échoue avec `EXDEV` dès que le code vit uniquement dans les couches de l'image Docker (`overlay2` refuse de renommer un répertoire encore uniquement présent dans une couche inférieure, vérifié même pour deux répertoires créés dans la même instruction `RUN`) OU sur deux bind-mounts hôte séparés (même disque physique, chaque `volumes:` déclaré ouvre son propre `st_dev`) — seule configuration vérifiée fonctionnelle : un volume Docker nommé unique couvrant tout `/app`, peuplé automatiquement par Docker au premier démarrage, avec `data/`/`logs/` bind-montés depuis l'hôte par-dessus pour rester directement éditables. Image republiée sur Docker Hub (`zdid2/dimotic-ha`) avec ce correctif, vérifiée en conditions réelles (activation/désactivation testée via le vrai `fs.renameSync` du conteneur). Ancienne version v4.21 archivée. |
| 03/08/2026 | **v4.21** | techniques-socle-ha-mqtt_specs_v4.21.md | Claude | **Réécriture complète de la §11 "Docker"** (demande utilisateur, "il faut me construire les Dockerfile et compose qui vont bien") — l'ancien contenu (Dockerfile/compose.yaml embarqués dans le texte) décrivait l'architecture pré-restructuration (pnpm, build racine unique), déjà obsolète et jamais corrigée jusqu'ici. Nouveau `Dockerfile`/`compose.yaml`/`docker/build-apps.sh` à la racine du projet, construits et testés en conditions réelles (build complet des 9 applications — core + 8 apps métier — dans un conteneur) : le code applicatif est monté depuis l'hôte plutôt que copié dans l'image (`fs.renameSync` entre `applications/` et `applications_désactivées/` pour l'activation/désactivation d'une application échoue entre deux systèmes de fichiers différents ; absence de build fiable à la racine du projet), `network_mode: host` (le broker MQTT et Home Assistant tournent en `localhost` sur la machine cible), `privileged: true` + volume `/dev` pour l'accès série de RFXCOM (préserve la robustesse de `PortDetector` face à la renumérotation du port, plutôt qu'un mappage de device figé qui la contournerait), exécution en utilisateur hôte plutôt qu'en root. Deux bugs latents réels, jusqu'ici invisibles côté hôte (masqués par un ancien `node_modules` racine jamais nettoyé), découverts et corrigés en isolant le build dans un conteneur : `arbreouquoi`/`nommage` utilisaient `zod` sans le déclarer dans leur propre `package.json` ; deux fichiers TypeScript navigateur de HAPLAN (`FloorPlan.ts`, `PositionManager.ts`) utilisaient le type `NodeJS.Timeout`, jamais résoluble côté navigateur, corrigé en `ReturnType<typeof setTimeout>`. Ancienne version v4.20 archivée. |
| 03/08/2026 | **v4.20** | techniques-socle-ha-mqtt_specs_v4.20.md | Claude | **Correction d'un crash total du process** (demande utilisateur, suite à TODO.md "MqttTransport.publish() lève une exception synchrone sur déconnexion transitoire — fait planter tout le process") : `MqttTransport.publish()`/`subscribe()` ne lèvent plus jamais pour une raison de connexion. Publications : file d'attente courte (200 messages max, expiration 30s), rejouée à la reconnexion. Abonnements : mémorisés et réappliqués à **chaque** reconnexion (pas seulement la première) — corrige au passage un défaut latent distinct découvert en implémentant ce correctif : chaque reconnexion recrée un client `mqtt.MqttClient` entièrement nouveau, qui ne conserve nativement aucun abonnement précédent, causant une perte silencieuse et définitive de la réception des commandes HA→app après la moindre coupure MQTT. Vérifié : build propre, suite de tests inchangée (mêmes échecs préexistants sans rapport, aucune régression introduite). Ancienne version v4.19 archivée. |
| 03/08/2026 | **v4.19** | techniques-socle-ha-mqtt_specs_v4.19.md | Claude | **Rattrapage de dérive code/specs** (aucun changement de comportement) : dédoublonnage des écouteurs `SocketBridge` par application (§5.4.2, corrige des diffusions serveur→client dupliquées pour tout module enregistrant ses événements plus d'une fois), énumération à jour des routes REST exceptionnelles au principe tout-Socket.io avec ajout de `HaAutomationBackupService` en 3ème exception (§5.7, nouvelle section), `ModuleContainer`/`Sidebar` (§6.1/6.2 nouvelles — verrou de chargement par module, cache évitant de rejouer les scripts d'un module déjà affiché, navigation réelle pour les applications à page dédiée), mécanisme réel des attributs de taxonomie en topic MQTT dédié (`json_attributes_topic`, §8.5.4) remplaçant deux tentatives antérieures jamais fonctionnelles (clé `extra` de la découverte, puis attributs d'état republiés à chaque changement), mécanisme de retrait de découverte MQTT (`unpublishDiscovery`), distinction `state_value_template`/`value_template` selon le composant HA, et repli sur texte brut à la réception d'une commande MQTT non-JSON (§8.5.2, HA envoie `"ON"`/`"OFF"` brut par défaut sur les schémas light/switch standard). Ancienne version v4.18 archivée. |
| 03/08/2026 | **v5.4** | recepteurs-emetteurs-rfxcom_specs_v5.4.md | Claude | **Rattrapage complet code/specs**, en écho au rattrapage de `fonctionnelles-rfxcom_specs`. Format réel du `uniqueId`/`deviceId` (`{protocole}_{subType}_{sensorId}[_{unitCode}]`), réponse définitive sur la persistance `lastOn`/`lastLevel` (écrits en YAML mais strippés au rechargement par le schéma Zod — cause racine confirmée de la rafale de commandes OFF à chaque redémarrage, §4.3), asymétrie `primaryEmitter`/`emitters[]` documentée (§4.4 — un récepteur n'écoute pas en écho son propre `primaryEmitter`), correction du slash initial erroné sur tous les exemples de topics (§8.5.2), détail complet de la découverte de scène avec les clés `type`/`subtype` requises par HA (§7.3), schéma Zod réel à jour (§5.3). Ancienne version v5.3 archivée. |
| 03/08/2026 | **v1.3** | implementation-rfxcom_specs_v1.3.md | Claude | **Réécriture complète** des sections décrivant l'API de la bibliothèque `rfxcom`, qui documentaient depuis l'origine une API fictive (pas d'événement générique `'device'`, pas de `'connect'`/`'error'` génériques, options du constructeur réduites à `{debug}`, dispatch par `switch` sur le protocole et non `instanceof`, échelle de dim réelle 0-15 et non 0-100/0-255) — écart déjà signalé dans le commentaire d'en-tête de `rfxcom.d.ts` mais jamais répercuté dans ce document. Nouvelle §8 "Persistance et Validation" documentant, avec preuve, la cause racine de la rafale de commandes OFF à chaque redémarrage. Section "Communication Inter-Applications" (jamais implémentée, doublon de numérotation) retirée au profit d'une annexe unique dans `fonctionnelles-rfxcom_specs_v5.9.md` §22.3. Ancienne version v1.2 archivée. |
| 03/08/2026 | **v5.9** | fonctionnelles-rfxcom_specs_v5.9.md | Claude | **Rattrapage complet du décrochage code/specs** constaté début août 2026 après plusieurs semaines de dérive : détection automatique du port série (§8.2, absente jusqu'ici), verrou d'ordonnancement démarrage→push protocoles matériel→découverte initiale avec filet de sécurité 20s (§8.3, corrige une course qui pouvait faire échouer le push), reconnexion à chaud propre à RFXCOM (§8.5), retrait de découverte MQTT à la désélection (§17.3), topic MQTT dédié aux attributs de taxonomie remplaçant un mécanisme jamais fonctionnel (§2.6/§17.2), format réel des identifiants (`protocole_subType_sensorId[_unitCode]`, pas `protocole_sensorId`, partout dans le document), absence d'état "unknown" fictif publié au démarrage (§9.2), arborescence réelle des programmes (§18, plate, sans les sous-dossiers documentés jusqu'ici), numérotation des sections corrigée, section Communication Inter-Applications (jamais implémentée) déplacée en annexe et explicitement marquée comme telle (§22.3). Nouvelles limitations documentées (§20) dont la cause racine de la rafale de commandes OFF à chaque redémarrage (`lastOn`/`lastLevel`/`lastValue` strippés au rechargement par le schéma Zod). Ancienne version v5.8 archivée. |
| 03/08/2026 | **v1.5** | fonctionnelles-ia_specs_v1.5.md | Claude | **Prise en compte du déclencheur réactif `state_change`** introduit par `fonctionnelles-planificateur_specs` v1.5 : §10 "La boucle de réinterprétation à l'exécution" étendue à quatre situations de déclenchement (ajout du changement d'état d'entité), mention du nouveau champ de contexte `triggered_entity_id` enrichissant la réinterprétation pour ce cas précis, §9 précise que `trigger.type` admet cette nouvelle valeur sans changer la structure du JSON échangé. Nouvelle limitation connue (§14) : ambiguïté récurrent/one-shot de "quand X, fais Y" non résolue côté classification `ia` — traité récurrent par défaut, sans clarification demandée à l'utilisateur (mécanisme de clarification à concevoir séparément, suivi dans TODO.md). Ancienne version v1.4 archivée (aucun changement de contenu identifié entre 1.3 et 1.4 au moment de cette révision). |
| 03/08/2026 | **v1.5** | fonctionnelles-planificateur_specs_v1.5.md | Claude | **Déclencheur réactif `state_change`** (nouvelle §3.2) : planifications sur changement d'état d'entité HA — précise (`entity_id`) ou par défaut sur tout un domaine (`domain`, ex: "toutes les lumières"), priorité entité>domaine, comportement "minuterie" (redéclencher une entité pendant une attente en cours l'annule et repart de zéro, suivi par couple planification/entité). §6 étendue à quatre situations de déclenchement, contexte de déploiement enrichi de `triggered_entity_id` pour le ciblage implicite d'une action sans lieu explicite. Nouvelle §5.1 : persistance d'une heure cible absolue (`next_fire_at`) et reprise sur le temps réellement restant après un redémarrage pour les triggers temporels (corrige au passage un comportement antérieur non documenté : `delay`/`duration` repartaient intégralement à zéro à chaque redémarrage), fenêtre de rattrapage configurable (`catchUpWindowSeconds`) au-delà de laquelle un déclenchement manqué est abandonné et marqué visiblement (`missed`, badge UI). Limitations connues ajoutées (§12) : reprise imprécise pour `state_change` après coupure (durée d'attente jamais persistable, dépend d'une réinterprétation Mistral), ambiguïté récurrent/one-shot de "quand X, fais Y" non résolue (traité récurrent par défaut). Testé en direct de bout en bout sur une vraie lumière RFXCOM (détection, réinterprétation, ciblage, minuterie, reprise, rattrapage). Ancienne version v1.4 archivée (aucun changement de contenu identifié entre 1.3 et 1.4 au moment de cette révision). |
| 30/07/2026 | **v4.18** | techniques-socle-ha-mqtt_specs_v4.18.md | Claude | **`AreaEnsureService` — création active des areas HA (§8.5.4)**, suite à la découverte que `device.suggested_area` (v4.17) n'est jamais fiable côté HA. `core` crée désormais lui-même l'area via l'API WebSocket (`config/area_registry/create`) avant de publier la découverte, entièrement transparent pour les 4 applications émettrices. Vérifié sur une installation HA neuve : 86/96 devices assignés automatiquement, 24 areas créées sans échec. |
| 30/07/2026 | **v4.17** | techniques-socle-ha-mqtt_specs_v4.17.md | Claude | **Ajout de `device.suggested_area` en découverte (§8.5.4)**, à la demande de l'utilisateur, alimenté avec le lieu de taxonomie (pas le lieu précis) dans les 4 applications qui envoient des messages de découverte (RFXCOM, EVOO7, AREXX, NOMMAGE passthrough). Remplace un ancien champ `area_id` déclaré mais jamais alimenté et de toute façon inexploitable (pas la bonne clé du schéma MQTT discovery de HA). Vérifié en conditions réelles : 94/96 messages de découverte portent la bonne valeur, les 2 exceptions correspondant à des données EVOO7 sans taxonomie saisie (comportement attendu). |
| 29/07/2026 | **v4.16** | techniques-socle-ha-mqtt_specs_v4.16.md | Claude | **Correction du format des topics état/commande/LWT (§8.5.4)** : ne doivent pas commencer par un `/` (slash initial = premier niveau de topic MQTT vide, non-standard) — erreur présente depuis la définition initiale du format (v4.8, 21/07/2026), repérée par l'utilisateur en inspectant directement les messages retenus sur le broker (`mosquitto_sub`) d'une nouvelle installation HA en cours de test. Corrigée à la source unique : les 4 fonctions `get*Topic`/`getBridgeStatusTopic` de `applications/core/src/ha/integration/types/ha-mqtt.ts` (une seule ligne modifiée chacune, suppression du `/` initial du template literal). Le topic de découverte (`homeassistant/{component}/{object_id}/config`) n'était pas concerné, il a toujours été correct. 25 tests unitaires mis à jour (assertions sur l'ancien format). Anciens topics retenus (avec `/` initial) explicitement effacés du broker de test avec un payload vide retenu après le correctif, pour éviter des entités dupliquées/fantômes côté HA — à refaire sur toute autre instance ayant déjà tourné avec l'ancien format avant de la basculer sur cette version. |
| 28/07/2026 | **v4.15** | techniques-socle-ha-mqtt_specs_v4.15.md | Claude | **Nouvelle §5.6 "Porte d'authentification OAuth2 HA (accès externe)"**, à la demande de l'utilisateur ("attaquons la mise en place oauth2"), suite à la conception "accès externe" discutée précédemment (HAPLAN, classification `audience`). Mécanisme optionnel, désactivé par défaut (`web.auth.enabled`), sans effet sur le déploiement interne actuel — prévu pour une future instance externe dédiée derrière un reverse proxy TLS. Le flux OAuth2 natif de HA (`/auth/authorize` + `/auth/token`, vérifié contre `developers.home-assistant.io/docs/auth_api`) sert uniquement de portail de connexion ; cookie de session signé HMAC-SHA256 (sans état côté serveur, aucune nouvelle dépendance npm) protégeant à la fois les routes REST (middleware de garde, premier enregistré) **et** la connexion Socket.io elle-même (`io.use()` sur le handshake) — les deux canaux étaient jusque-là sans authentification. Pas de permissions différenciées par utilisateur HA (choix explicite tranché avec l'utilisateur) : le serveur garde son propre jeton longue durée pour piloter HA. Nouveau `AuthService` (`applications/core/src/infrastructure/auth/`), intégré dans `PresentationServer` et `SocketBridge`. Vérifié sur une instance de test isolée (scratch, port séparé, sans toucher au serveur/config réel) : garde REST + Socket.io actives, état OAuth2/cookie forgé ou expiré rejetés, cookie valide accepté avec accès aux données réelles ; comportement du déploiement interne inchangé (`web.auth` absent de la config réelle). |
| 24/07/2026 | **v4.14** | techniques-socle-ha-mqtt_specs_v4.14.md | Claude | **Restructuration `data/` en un sous-répertoire par application (§4.1, §7)**, à la demande de l'utilisateur ("chaque application ait son répertoire... facilitera par la suite le déplacement d'une application d'une machine à une autre") : le fichier unique `data/config.yaml` devient `data/core/config.yaml` (`ha`/`web`/`logging`) + un `data/{app}/config.yaml` par application (objet nu, pas de clé d'app en tête). Tout reste sous `data/`, seul répertoire monté en volume Docker externe. `ConfigLoader`/`ConfigWriter` gagnent un `appDataRoot` optionnel (scan+fusion au chargement, écriture isolée par fichier) ; `ConfigService.getConfig()` et le reste de son API publique gardent une forme identique en mémoire — `IAppConfigProvider`/code métier des applications dérivées inchangés. Bonus structurel : `savePartialConfig()` met désormais à jour `this.config` en mémoire après écriture (aligné sur `saveModuleConfig()`), éliminant un `reload()` manuel jusqu'ici nécessaire côté appelant. Fichiers de données existants déplacés sans renommage. Vérifié en conditions réelles : isolation d'écriture confirmée par comparaison de timestamps (`stat`) avant/après sauvegarde sur RFXCOM et sur la config socle — seul le fichier de l'application concernée change. |
| 22/07/2026 | **v4.13** | techniques-socle-ha-mqtt_specs_v4.13.md | Claude | **Correction §6 "Couche Présentation"** suite à un audit de cohérence des specs après la réintroduction d'Alpine.js (`presentation_specs_v4.0.md`, `alpinejs-implementation_specs_v1.0.md`) : la liste des invariants excluait encore explicitement Alpine.js ("Aucun framework UI tiers... Alpine.js"), affirmation devenue fausse. Corrigée pour autoriser Alpine.js sous réserve du respect strict d'`alpinejs-implementation_specs_v1.0.md`, référence vers `presentation_specs` mise à jour v3.2→v4.0, note historique v4.7 annotée obsolète. |
| 22/07/2026 | **v4.12** | techniques-socle-ha-mqtt_specs_v4.12.md | Claude | **Remplacement du client WebSocket HA maison (`HaWsTransport.ts`+`HaWsClient.ts`) par `home-assistant-js-websocket`** (lib officiellement maintenue par l'équipe Home Assistant) — demandé par l'utilisateur après avoir constaté que la connexion HA WS bouclait en reconnexion toutes les ~1s avec un jeton invalide. Root cause : `HaWsTransport` envoyait sa propre authentification à l'ouverture du socket, en plus de celle envoyée par `HaWsClient` en réponse à `auth_required` — double authentification par connexion (un premier correctif partiel avait déjà supprimé le doublon côté `HaWsClient`, celui-ci supprime le vrai fautif côté transport en le remplaçant entièrement). `HaWsTransport.ts` supprimé, `HaWsClient` garde la même API publique (`connect`/`disconnect`/`loadInitialRegistry`/`subscribeToEvents`/`sendCommand`/`onConnect`/`onDisconnect`/`onError`/`onStateChanged`/`onAreaUpdated`/`onDeviceUpdated`/`onEntityUpdated`/`reconfigure`) — `onMessage` supprimé (pas d'équivalent officiel, son unique consommateur `HaCommandService` faisait un ré-appariement par id déjà redondant et bugué, simplifié pour résoudre directement depuis la Promise de `sendCommand`). Bonus de la lib officielle : renonce immédiatement sur jeton invalide (`auth_invalid`) au lieu de boucler indéfiniment. Tests de `HaWsClient` entièrement réécrits (étaient 100% cassés avant ce chantier, 0/24 passants — 16/16 passants maintenant). Vérifié en conditions réelles avec le jeton HA fraîchement régénéré par l'utilisateur : connexion et authentification en un seul message, plus de boucle. |
| 21/07/2026 | **v4.11** | techniques-socle-ha-mqtt_specs_v4.11.md | Claude | **Correction d'un gap fonctionnel réel (§8.5.2), découvert en implémentant les scènes RFXCOM** : `publishDiscoveryFor` abonne désormais automatiquement le bridge au topic de commande d'une entité `commandEnabled: true`. Avant ce correctif, aucun module métier n'appelait jamais `subscribeCommandsFor` — les commandes HA→app n'étaient donc **jamais reçues en pratique** pour aucun module utilisant la découverte normale (§8.5.0), bien que le code de réception/routage soit par ailleurs correct. Non détecté plus tôt car seule la réception RF433→MQTT avait été testée en conditions réelles, jamais le sens HA→device. |
| 21/07/2026 | **v1.5** | guide-nouvelle-application_specs_v1.5.md | Claude | **Correction de la §3.9 (import de `SocketService`)** : l'exemple pointait vers un chemin qui n'a jamais existé (`core/src/presentation/services/SocketService`) et suggérait un singleton `getInstance()` que la classe réelle n'a jamais eu — écart découvert en creusant l'implémentation réelle de `nommage` et `arbreouquoi`, où le même `@ts-ignore`/mauvais chemin s'était propagé. Nouvel exemple : import depuis `core/src/ui-exports.ts` (jamais `exports.ts`), `new SocketService()`, deux variantes de chemin selon le `rootDir` TypeScript de l'application (source directe vs `dist` UI du core déjà buildé). Référence croisée mise à jour vers `techniques-socle-ha-mqtt_specs_v4.10.md`. |
| 21/07/2026 | **v4.10** | techniques-socle-ha-mqtt_specs_v4.10.md | Claude | **Séparation `exports.ts` (backend) / `ui-exports.ts` (navigateur)** (§4.2.1, suite à la question utilisateur « le core met-il ses objets dans dist et pas dans src ? ») : `SocketService` (code navigateur — `window`, `document`, script global `socket.io-client`) retiré de `exports.ts` et déplacé dans le nouveau `src/ui-exports.ts`, exclu du build backend du core (`tsconfig.json` du core). Corrige une pollution constatée concrètement dans `nommage` et `arbreouquoi` : leur `dist` backend contenait une copie morte et inexécutable de `SocketService.js`, simplement parce qu'ils importaient des types backend depuis `exports.ts` (une seule unité de compilation TypeScript — tout import, même `import type`, force la résolution de tout le graphe du fichier). `arbreouquoi` contournait le problème par un `@ts-ignore` qui masquait en réalité DEUX bugs distincts : un chemin d'import incorrect (profondeur de `../` erronée) et un appel à `SocketService.getInstance()` qui n'a jamais existé sur la classe réelle — les deux corrigés au passage. `tsconfig.ui.json` du core gagne `declaration: true`/`declarationMap: true` pour permettre aux applications à `rootDir` restreint (ex: `arbreouquoi`) de consommer le `dist` UI du core avec des types corrects. |
| 21/07/2026 | **v4.9** | techniques-socle-ha-mqtt_specs_v4.9.md | Claude | **Ajout du Passthrough MQTT (§8.5.6, à la demande de l'utilisateur)** : mécanisme pour les applications qui relaient des messages MQTT déjà formés (lus depuis une source tierce — ex: `nommage` relayant des découvertes Zigbee2MQTT publiées sur un préfixe étranger) vers le broker HA unique, sans passer par la normalisation de découverte §8.5.0. Mode "réécriture de préfixe" (`integration:{module}:passthrough:discovery`, remplace le 1er segment de `sourceTopic` par `homeassistant`, QoS1/retain true) et mode "complet" (`integration:{module}:passthrough:publish`, topic+payload intégraux, aucune transformation). Nouvelles méthodes `publishDiscoveryPassthrough`/`publishPassthrough` sur `HaMqttIntegrationService`. La v4.8 (créée puis immédiatement remplacée dans la même session, jamais committée) n'a pas d'entrée séparée. |
| 21/07/2026 | **v4.8** | techniques-socle-ha-mqtt_specs_v4.8.md (superseded) | Claude | **Précision du format MQTT §8.5** (à la demande de l'utilisateur, suite à un audit du socle réel vs specs qui a révélé des divergences importantes côté MQTT) : un seul broker MQTT pour tout le socle ; mission par intégration = LWT + découverte persistante + état + réception des commandes ; nouveau concept **`bridge_instance`** (une intégration peut piloter plusieurs bridges physiques/logiques, chacun avec son propre LWT) ; nouveaux formats de topics état/commande `/{moduleName}/{bridgeInstance}/{deviceId}/state\|set` (remplacent l'ancien schéma générique `homeassistant/.../state\|set` et l'ancien LWT `ha-integration/{module}/status`) ; le topic de **découverte** HA standard (`homeassistant/{component}/{object_id}/config`) reste inchangé ; `deviceId` est un identifiant **opaque au socle**, dont l'encodage exact est défini par chaque module d'intégration (voir `recepteurs-emetteurs-rfxcom_specs` pour RFXCOM) ; clarification de la répartition des responsabilités : le module métier ne fournit que les données essentielles, `HaMqttIntegrationService` normalise et complète l'intégralité du message de découverte. La v4.7 (créée puis immédiatement remplacée dans la même session, jamais committée) n'a pas d'entrée séparée. |
| 21/07/2026 | **v4.7** | techniques-socle-ha-mqtt_specs_v4.7.md (superseded) | Claude | **Nettoyage Alpine.js + correction de version** : (1) Le fichier porte enfin le nom `v4.7` — l'entrée du 20/07/2026 ci-dessous avait déjà documenté ce numéro de version alors que le fichier était resté nommé `v4.6.md`, cette incohérence est corrigée par renommage (ancien fichier archivé dans `specs/archives/`). (2) Suppression de toutes les références **actives** à Alpine.js (stack technique §2, diagrammes d'architecture, structure de répertoires, dépendances npm) : la couche Présentation utilise TypeScript pur + Web Components natifs depuis `presentation_specs` v3.0. La section 6 ne duplique plus cette implémentation et renvoie vers `presentation_specs_v3.2.md`. Les mentions historiques d'Alpine.js dans le changelog interne du document (v4.2) sont conservées. Références croisées mises à jour dans les specs qui pointaient vers `techniques-socle-ha-mqtt_specs_v4.6.md`. |
| 20/07/2026 | **v4.7** | techniques-socle-ha-mqtt_specs_v4.6.md | Human | **Ajout du système de communication inter-applications** : Intégration du pattern Request/Reply asynchrone avec corrélation via EventBus partagé. Toutes les applications (sauf core) DOIVENT déclarer leurs capacités via `ApplicationCapabilities`. Ajout du fichier `exports.ts` dans le core pour centraliser les exports. *(Note 21/07/2026 : le fichier n'avait jamais été renommé en v4.7 malgré ce changelog — corrigé ci-dessus.)* |
| 20/07/2026 | **v1.0** | inter-app-communication_specs_v1.0.md | Human | **Nouvelle spécification** : Définition complète du système de communication inter-applications. Pattern Request/Reply avec corrélation obligatoire via `requestId`. Déclaration explicite des capacités par chaque application. Gestion asynchrone avec Promises et timeouts. |
| 19/07/2026 | **v4.6** | techniques-socle-ha-mqtt_specs_v4.6.md | Mistral Vibe | **Restructuration complète de l'arborescence** : Déplacement de `src/applications/` et `src/applications_desactivees/` vers `applications/` et `applications_desactivees/` à la racine du projet. Chaque application (y compris le core) est maintenant autonome avec ses propres `package.json`, `tsconfig.json`, et `dist/`. **Le core ne peut pas être désactivé**. Activation/désactivation via sous-menu Paramètres Techniques. Mise à jour des mécanismes de détection et d'activation/désactivation via `AppService`. |
| 18/07/2026 | v5.5 | fonctionnelles-rfxcom_specs_v5.5.md | Mistral Vibe | Spécifications Cover avec Lighting2, traduction Commandes HA→RFXCOM par type, arborescence des programmes, détection automatique, actions MQTT par le socle, indicateur connexion, traces détaillées |
| 17/07/2026 | **v4.5** | techniques-socle-ha-mqtt_specs_v4.5.md | Mistral Vibe | Ajout du **démarrage automatique des services d'application** avec reconnexion sur changement de configuration. **Traces obligatoires** au démarrage et gestion des erreurs de connexion (matériel absent, mauvais paramètres). Processus reproductible pour toute nouvelle application. |
| 14/07/2026 | **v4.4** | techniques-socle-ha-mqtt_specs_v4.4.md | Mistral Vibe | Ajout de la **gestion dynamique d'activation/désactivation** des applications via répertoires `src/applications/` et `src/applications_desactivees/`. |
| 10/07/2026 | v4.3 | techniques-socle-ha-mqtt_specs_v4.3.md | - | Restructuration de la configuration HA avec flags `ws_enable` et `mqtt_enable` + regroupement MQTT sous HA. |

---

### 📌 Spécifications Projet

| Date | Version | Fichier | Auteur | Changements |
|------|---------|---------|--------|-------------|
| 13/08/2026 | **v1.5** | PROMPT_PROJET.md | Claude | **Ajout de 2 lignes à la table §11** pour RPIGPIO et TELEINFO, créées le 12/08/2026 avec leur spec fonctionnelle dédiée. Aucun autre changement. Ancienne version v1.4 archivée dans `specs/archives/PROMPT_PROJET_v1.4-20260804.md`. |
| 04/08/2026 | **v1.4** | PROMPT_PROJET.md | Claude | **Correction de la table §11 "Pour Applications Spécifiques"**, désynchronisée depuis sa création — ne citait ni NOMMAGE, ni ARBREOUQUOI, ni IA, ni PLANIFICATEUR, ni AREXX, ni HAPLAN alors que ces 6 applications ont chacune au moins une spec fonctionnelle dédiée. 6 lignes ajoutées, aucun autre changement. Ancienne version v1.3 archivée dans `specs/archives/PROMPT_PROJET_v1.3-20260804.md`. |
| 21/07/2026 | **v1.3** | PROMPT_PROJET.md | Claude | **(1) Correction des références obsolètes** vers les specs suite au nettoyage Alpine.js et à l'audit de nommage du 21/07/2026 (renommages `v4.6`→`v4.7`, `v3.0`→`v3.2`, `v1.3`→`v1.4`, `v1.0`→`v1.2`, `v1.0`→`v1.1`, tiret→underscore). **(2) Passage des références de specs à un format sans numéro de version** (`techniques-socle-ha-mqtt_specs` au lieu de `techniques-socle-ha-mqtt_specs_v4.7.md`) dans les tableaux §11 et la structure recommandée, pour ne plus se périmer à chaque bump de version — même traitement appliqué à `PROMPT.md`. Ajout d'une entrée pour `inter-app-communication_specs`. Précision sur la convention de nommage à deux niveaux réellement en usage. Ancienne version v1.2 archivée depuis git HEAD dans `specs/archives/PROMPT_PROJET_v1.2-20260721.md`. |
| 19/07/2026 | **v1.2** | PROMPT_PROJET_v1.2.md | Mistral Vibe | **Ajout sections Gestion des Spécifications** : Identification des redondances majeures (EventBus, Architecture 5 couches, Gestion des erreurs, etc.) avec tableau des fichiers de référence. **Liste complète des specs nécessaires par type de développement** avec ordre de lecture recommandé. |
| 19/07/2026 | **v1.1** | PROMPT_PROJET_v1.1.md | Mistral Vibe | **Mise à jour de la structure des répertoires** : Intégration de la nouvelle arborescence avec `applications/` et `applications_desactivees/` à la racine. **Le core ne peut pas être désactivé**. Précision sur le sous-menu des **Paramètres Techniques** pour activer/désactiver les applications. |

---

### 📌 Autres Spécifications

| Date | Version | Fichier | Auteur | Changements |
|------|---------|---------|--------|-------------|
| 26/08/2026 | **v1.10** | fonctionnelles-planificateur_specs_v1.10.md | Claude | **Nouveau déclencheur `sun`** (§3.1bis) : lever/coucher du soleil, offset signé optionnel, réutilise `days`/`except_days` du trigger `recurrence`. Calcul réel via `suncalc` (nouvelle dépendance) plutôt qu'une lecture de `sun.sun` de HA, insuffisante combinée à un filtre de jours restrictif (calculer le coucher de samedi prochain depuis un dimanche demande une date arbitraire, pas "le prochain"). Position GPS de l'installation HA lue une fois via nouveau `HaBridgeClient.getHaConfig()` et mise en cache indéfiniment ; `scheduler.ts` reste pur/synchrone (position déjà résolue injectée en paramètre). `isRecurring()` étendu à `'sun'` — mêmes garanties de reprise après coupure que les autres déclencheurs récurrents. Demande utilisateur explicite ("le lever et le coucher de soleil sont utilisés dans toutes les implémentations de ma domotique, sauf chez moi"), reprend le calcul déjà résolu par l'utilisateur dans son ancien système (`suncalc`, retrouvé dans `zdidnodedomoutil/heurelevercouchersoleil.js`). Voir aussi `fonctionnelles-ia_specs_v1.12.md` pour le gabarit d'interprétation correspondant. Ancienne version v1.9 archivée. |
| 26/08/2026 | **v1.9** | fonctionnelles-planificateur_specs_v1.9.md | Claude | **Correctif §5.1** : un trigger récurrent manqué au-delà de la fenêtre de rattrapage n'était jamais reprogrammé (`resumeOrSchedule()` marquait `missed: true` mais ne réarmait aucun minuteur pour un récurrent, contrairement au cas non récurrent qui terminait proprement via `completed_at`) — restait actif mais silencieux indéfiniment. Bug réel signalé par l'utilisateur ("éteindre toutes les lumières tous les jours à 2h30", plus jamais déclenchée après un arrêt de service au moment précis du tir). Corrigé : reprogrammation immédiate pour la prochaine occurrence dans ce cas, `missed` reste affiché jusqu'au prochain déclenchement réussi comme avant. Vérifié en conditions réelles sur la planification concernée. Ancienne version v1.8 archivée. |
| 27/08/2026 | **v1.13** | fonctionnelles-ia_specs_v1.13.md | Claude | **Lieu unique déduit du `quoi`** (§16.6bis) : quand une phrase ne capture aucun lieu et que le `quoi` reconnu n'existe qu'à un seul `lieu_principal` distinct dans toute la maison ("allume le poêle"), ce lieu est utilisé automatiquement — prioritaire sur `context.lieuOrigine` (non branché en pratique de toute façon). Niveau `lieu_principal`, pas `lieu_precis`. Nouveau `liveCatalogs.ts::buildQuoiUniqueLieu()` + `resolveDefaultLieu()`, appliqués à `buildActionParams`/`si_alors`, volontairement pas à `donne`. Vérifié en conditions réelles : "allume le poele" → `lieux:["salle à manger"]`, 0 token. Corpus de test étendu à 59 cas. Demande utilisateur explicite. Ancienne version v1.12 archivée. |
| 26/08/2026 | **v1.12** | fonctionnelles-ia_specs_v1.12.md | Claude | **Gabarit `soleil`** (§16.4, lever/coucher du soleil) : pattern composable avec les fragments de jours existants (ex. "le week end une heure après le coucher du soleil ferme le volet"). Produit `trigger.type: 'sun'`, calculé réellement côté `planificateur` via `suncalc` — pas une lecture de `sun.sun` de HA (ne donne que le PROCHAIN lever/coucher, insuffisant combiné à un filtre de jours). Précédemment "hors périmètre" (v1.10/v1.11) ; demande utilisateur explicite ("utilisé dans toutes les implémentations domotiques sauf chez moi"), l'utilisateur avait déjà résolu ce même problème dans son ancien système via la même bibliothèque (`suncalc`). **Correctif jours FR→EN trouvé en préparant ce gabarit** : les enums `jour`/`week_end`/`jours_ouvres` capturaient des valeurs françaises alors que `scheduler.ts::triggerToMs` (et `regles_mistral.txt`) attendent des clés anglaises — un filtre de jour produit par l'interpréteur ne matchait *jamais* aucun jour avant ce correctif, silencieusement. Second bug lié corrigé au passage (`touslesjours`+`jours_ouvres`, capture sous la mauvaise clé). Corpus de test étendu à 58 cas. Toutes demandes utilisateur, session du 26/08/2026. Ancienne version v1.11 archivée. |
| 26/08/2026 | **v1.11** | fonctionnelles-ia_specs_v1.11.md | Claude | **Gabarits restants de l'interpréteur déterministe** (§16.4) — fusion `jusqua`/`de` dans `a`, `entre` (fenêtre, `trigger.type: 'window'`, déjà supporté par `scheduler.ts::triggerToMs`), `donne` (interrogation, routée vers la résolution d'entités existante — pas un portage de `donnemoi.js`), exclusion `sauf <lieu>*` intégrée à `ordre_immediat`. **Correctif d'un cas limite réel trouvé en test sur l'instance locale** : une exclusion qui vide entièrement une liste de lieux explicitement nommés ("allume le salon sauf le salon", contradiction littérale) produisait `lieux: []`, que `HaStructureRegistry.getEntitiesByQuoiAndLieux` interprète comme "aucun filtre" — ciblait par erreur *toute la maison* (vérifié en direct : a bien déclenché l'ensemble des lumières). Corrigé : repli Mistral dans ce cas précis plutôt que ce faux "partout". `levercouchersoleil`/`levercouchersoleil1`/`delayrepeat` confirmés hors périmètre (`triggerSchema`/`scheduler.ts` de `planificateur` n'ont ni heure relative au soleil ni intervalle répétitif fonctionnel — `trigger.every` existe mais n'est lu nulle part). **Cache des 100 dernières phrases + comptabilisation cache/interpréteur/Mistral** (§16.10, nouveau, demande utilisateur) : `PhraseCache`/`InterpreterMetrics` partagés entre le chat direct (`IaService`) et la réinterprétation d'un déclenchement planifié (`DeployResponder`, même ordre cache→interpréteur→Mistral, `liveCatalogs.ts` factorisé pour éviter la duplication) ; gelés pendant un échange d'assistance Mistral en cours (`isFreshExchange()`) pour ne jamais intercepter une clarification en cours de dialogue. Corpus de test étendu à 53 cas (`applications/ia/interpreter/tester.mjs`). Toutes demandes utilisateur, session du 26/08/2026. Ancienne version v1.10 archivée. |
| 26/08/2026 | **v1.10** | fonctionnelles-ia_specs_v1.10.md | Claude | **Interpréteur déterministe français** (§16, nouveau) — reprend la logique de l'ancien moteur pré-dimotic-ha `zdidnodedomotext` (`interpretetext.js`/`modelesv2.js`/`num_convert_date_duration_time.js`, analysé à la demande de l'utilisateur), réécrite en TypeScript, corrigée du bug de découpage de phrases (vérifié empiriquement : une phrase se terminant par un nombre entier n'était jamais coupée de la suivante), et convergeant vers les structures déjà produites par Mistral (`ExecuterActionParams`/`PlanificationDefinition`/`MacroDefinition`) plutôt qu'un nouveau format. Pré-filtre dans `IaService.handleChat()` avant tout appel Mistral : reconnaissance confiante → exécution directe (`ToolExecutor.executeDirect()`) ; sinon repli intégral inchangé sur le chemin Mistral existant (§7-§10). Vocabulaire/gabarits externalisés en YAML éditables à chaud (même convention que `regles_mistral.txt`). Nouveau gabarit événementiel `si_alors` (absent du legacy, demande explicite utilisateur) ciblant `trigger.type: 'state_change'`, déjà géré par `StateWatcher.ts` côté `planificateur` — aucune nouvelle mécanique d'exécution requise. Conception validée en session (nombreuses vérifications directes du code réel avant chaque décision — voir §16 pour le détail), implémentation en cours. Ancienne version v1.9 archivée. |
| 16/08/2026 | **v1.3** | fonctionnelles-evoo7_specs_v1.3.md | Claude | **⭐ Remplacement complet du transport vers le boîtier** : MQTT (broker EVOO7 dédié + traducteur externe) remplacé par une connexion Socket.IO directe, le vrai protocole natif du boîtier — découvert en analysant un ancien projet de traduction Socket.IO↔MQTT de l'utilisateur. Nouveau §13 : protocole natif détaillé (identification MD5, réception `datas` en bloc, `sendUpdate()`, file d'attente stricte des commandes), vérifié en conditions réelles en lecture ET en écriture (avec confirmation physique indépendante de l'utilisateur sur l'écran du boîtier). Config revue (§4) : `box.{address,port,user,password}` remplace `mqtt.*`/`topicCommand`/`formatMessageCommand`. Limitations `$date$`/`formatMessageSensor` de la v1.2 devenues sans objet (`evoo7-templates.ts` supprimé). Sauvegarde complète faite avant modification (`backups/evoo7_2026-08-16/`). Ancienne version v1.2 archivée. |
| 16/08/2026 | **v1.1** | fonctionnelles-rpigpio_specs_v1.1.md | Claude | Migration en process séparé (§2.4, IPC — voir `fonctionnelles-supervisor_specs` v2.6) + présence de l'agent mqtt-io distant, lecture seule (§7.3, nouveau — `RpigpioService` s'abonne au LWT natif de mqtt-io). Non vérifié en conditions réelles sur `ha2` (redéploiement du conteneur mqtt-io non autorisé cette session). Ancienne version v1.0 archivée. |
| 16/08/2026 | **v1.1** | fonctionnelles-teleinfo_specs_v1.1.md | Claude | Migration du composant dimotic-ha en process séparé (§2.4, IPC — l'agent RPi1 reste un déploiement SSH+systemd distinct). Présence de l'agent RPi1 : LWT + battement de cœur ajoutés à `ha-publisher.js` (§6.5, nouveau) — seule implémentation de présence/heartbeat de cette session vérifiée en conditions réelles sur le matériel physique. Ancienne version v1.0 archivée. |
| 12/08/2026 | **v1.0** | fonctionnelles-teleinfo_specs_v1.0.md | Claude | **Nouvelle application TELEINFO** : paramétrage de 2 compteurs EDF mode historique (ADCO, quoi/où) câblés sur un RPi1 via bascule GPIO matérielle, déploiement SSH+systemd d'un agent Node.js adapté d'un programme existant de l'utilisateur (protocole série/checksum/bascule GPIO repris à l'identique, déjà prouvés sur ce matériel — pas de Docker, RPi1 en ARMv6 incompatible avec les builds Node officiels récents). Deux bugs réels trouvés et corrigés en déploiement réel : découpage de trame à un seul niveau (aucune trame jamais reconnue malgré des données valides sur le fil, §4.3), rythme de publication non throttlé (~5s/compteur, ramené à 30s configurable après coup, §5.4). Session du 12-13/08/2026. |
| 12/08/2026 | **v1.0** | fonctionnelles-rpigpio_specs_v1.0.md | Claude | **Nouvelle application RPIGPIO** : paramétrage de pins GPIO (quoi/où, inversion, direction) exposées via l'outil tiers mqtt-io, déploiement Docker sur ha2/orangepi (device HA distinct par pin, override nécessaire — mqtt-io regroupe sinon toutes ses pins sous un device unique). Bug réel trouvé en déploiement réel : `privileged: true` seul insuffisant pour l'accès `/dev/mem` (l'image bascule sur un utilisateur non-root en interne), corrigé par `user: '0:0'` dans le compose généré. Session du 12/08/2026. |
| 06/08/2026 | **v1.1** | fonctionnelles-haplan_specs_v1.1.md | Claude | **Échelle icônes/textes réglable par l'utilisateur** (§12.2) — après plusieurs allers-retours sur la taille des icônes/textes du plan (jugée tour à tour trop petite puis "énorme"), toutes les règles de taille scalables de `styles.css` sont désormais pilotées par une seule variable `--plan-scale`, réglable via un curseur (60-120 %) ouvert par un nouveau bouton loupe des contrôles flottants, mémorisé par écran (`localStorage`, pas le formulaire générique de paramètres — jamais atteint pour cette app, voir §7.1/§12.2) plutôt que par utilisateur, cohérent avec l'usage prévu d'un plan mural fixe. **Navigation circulaire par flèches entre plans** (§12.1), en remplacement pratique du sélecteur cachée dans le menu replié. **Affichage d'état simplifié pour VMC/chauffe-eau/radiateur** (§9.3) : le libellé ON/OFF (déjà cassé pour le radiateur — bug #3 de la v1.0 — et de toute façon de taille figée en JS, ignorant `--plan-scale`) est retiré des trois plutôt que corrigé ; l'état s'y lit désormais uniquement à la couleur de l'icône (chauffe-eau bleu/orange, VMC blanc/jaune, radiateur bleu/rouge). Bug #3 (§15.3) révisé en conséquence : ne concerne plus que l'interrupteur générique (`EnhancedSwitchObject`). Ancienne version v1.0 archivée. |
| 03/08/2026 | **v1.0** | fonctionnelles-haplan_specs_v1.0.md | Claude | **Première spécification formelle**, écrite a posteriori — HAPLAN (plans de maison tactiles, pilotage d'entités HA existantes) est opérationnelle depuis fin juillet 2026 sans aucune documentation dédiée jusqu'ici. Couvre l'architecture (pas de découverte MQTT, Mode A uniquement), le backend `HaplanService`, la persistance des plans/positions (toujours la liste complète, jamais un delta), l'upload de plan (2ème exception REST du socle), l'arbre de taxonomie pour le sélecteur d'entités, l'accès externe/page dédiée (routage réel via `Sidebar`, pas SPA), le modèle d'objets HA porté de l'ancien projet standalone `haplanserver` (hiérarchie de classes, détection de type, 5 fenêtres contextuelles), le nom d'entité dérivé de la taxonomie (bandeau flottant + titres de fenêtre), le menu escamotable. Liste consolidée de 16 bugs fonctionnels identifiés en lisant le code réel (variateur de lumière inopérant, statuts d'interrupteur jamais mis à jour, reclassement erroné possible d'une lumière en VMC/radiateur, absence de support tactile malgré la présentation "tactile" de l'application, etc.) et du code mort hérité du portage. |
| 03/08/2026 | **v1.0** | fonctionnelles-arexx_specs_v1.0.md | Claude | **Première spécification formelle**, écrite a posteriori — AREXX (capteurs température/humidité BS1000/BS500) est opérationnelle depuis le 23/07/2026 sans documentation dédiée jusqu'ici. Couvre l'architecture volontairement minimale (application en lecture seule, pas de récepteurs/scènes/commandes), les 3 modes d'acquisition (push/poll/usb — dont un binaire ARM vendored spawné en process enfant pour le mode USB), le serveur HTTP local, le registre des capteurs et sa persistance opportuniste, la taxonomie et la découverte HA (topic d'attributs dédié, comme RFXCOM/EVOO7), la configuration, l'UI/Socket.io. Liste consolidée de limitations connues identifiées en lisant le code réel (aucun retrait de découverte MQTT, `stop()` ne désabonne pas ses écouteurs, `usbDevicePath` jamais lu, `rulefile.txt` du binaire USB cible un port codé en dur, `arexx:error` jamais émis, etc.). |
| 27/07/2026 | **v5.8** | fonctionnelles-rfxcom_specs_v5.8.md | Claude | **Remplacement complet de la gestion des protocoles (§8.2)** : l'ancien filtre logiciel après décodage (v5.4, granularité Lighting1/2/4/5/6/Blinds1/RFXSensor/RFXMeter, événements `rfxcom:protocol:toggle`/`rfxcom:protocols:update`) est retiré à la demande de l'utilisateur ("supprimer ce filtre logiciel qui risque de demander un niveau d'expertise élevée sans aucun soutien sur internet") et remplacé par un filtre **matériel** unique : notre sélection persistée (`enabledHardwareProtocols`, granularité fine de la bibliothèque `rfxcom` — ex: X10/ARC/AC/OREGON) reste la seule référence, jamais écrasée par ce que rapporte le matériel à la connexion, et poussée en RAM uniquement (jamais EEPROM, jamais `saveRFXProtocols`) — d'abord automatiquement une fois par session (verrou anti-boucle, un push déclenchant en retour un nouvel événement `status`), puis à la demande via un bouton dédié "Envoyer au RFXtrx433" qui pousse toute la sélection courante en un seul lot plutôt qu'à chaque coche. Nouveau bouton "Rafraîchir" (commande "get status" de la bibliothèque, sans reconnexion). Limitation documentée et acceptée par l'utilisateur : `RFXMeter` n'a aucun bit matériel correspondant, donc plus aucun filtrage possible pour cette catégorie. Documente aussi l'onglet Protocoles (absent jusqu'ici de la liste des onglets), la taxonomie Devices/Récepteurs saisie en 5 champs séparés avec sauvegarde individuelle (fenêtres modales), et les libellés lisibles dérivés de la taxonomie dans les listes déroulantes d'émetteurs. Ancienne version v5.7 archivée. |
| 27/07/2026 | **v5.3** | recepteurs-emetteurs-rfxcom_specs_v5.3.md | Claude | **Mise à jour du workflow UI (§11.3)** pour refléter l'interface réelle (contrat Socket.io inchangé) : taxonomie saisie en 5 champs séparés avec sauvegarde individuelle plutôt qu'un champ OÙ unique, formulaires en fenêtre modale, liste déroulante d'émetteurs (primaryEmitter/emitters) affichant un libellé lisible dérivé de la taxonomie plutôt que le uniqueId brut. Détail complet de cette UI dans `fonctionnelles-rfxcom_specs_v5.8.md` §12.2. Ancienne version v5.2 archivée. |
| 24/07/2026 | **v1.1** | fonctionnelles-evoo7_specs_v1.1.md | Claude | **Correction de chemins uniquement**, suite à la restructuration `data/` (`techniques-socle-ha-mqtt_specs_v4.14.md` §7) : `data/config-evoo7-donnees-v1.0.yaml` → `data/evoo7/config-evoo7-donnees-v1.0.yaml`, section `evoo7` de l'ancien `data/config.yaml` → `data/evoo7/config.yaml` (objet nu). Aucun changement de contenu propre à EVOO7. |
| 24/07/2026 | **v1.4** | fonctionnelles-planificateur_specs_v1.4.md | Claude | **Correction de chemins uniquement**, suite à la restructuration `data/` : les deux fichiers YAML de stockage (§5) et la section `planificateur` de la config → `data/planificateur/`. Aucun changement de contenu propre à `planificateur`. |
| 24/07/2026 | **v1.4** | fonctionnelles-ia_specs_v1.4.md | Claude | **Correction de chemin uniquement** (§12) : section `ia` de l'ancien `data/config.yaml` → `data/ia/config.yaml` (objet nu). Aucun changement de contenu propre à `ia`. |
| 24/07/2026 | **v1.3** | fonctionnelles-nommage_specs_v1.3.md | Claude | **Correction de chemins uniquement** (§3, §7.1), suite à la restructuration `data/` : `data/config.yaml` → `data/nommage/config.yaml`, exemple YAML mis à jour en objet nu (suppression de la clé `nommage:` d'enveloppe, devenue redondante avec le nom du dossier). Aucun changement de contenu propre à NOMMAGE. |
| 24/07/2026 | **v1.1** | fonctionnelles-arbreouquoi_specs_v1.1.md | Claude | **Correction de chemin uniquement** (C-009, §8.2) : `data/config.yaml` → `data/arbreouquoi/config.yaml`. |
| 24/07/2026 | **v1.1** | implementation-arbreouquoi_specs_v1.1.md | Claude | **Correction de chemins uniquement** (§8.1, §8.3) : `data/config.yaml` → `data/arbreouquoi/config.yaml`, exemple YAML mis à jour en objet nu (le socle et sa propre section vivent désormais dans des fichiers séparés). |
| 24/07/2026 | **v1.3** | implementation-nommage_specs_v1.3.md | Claude | **Correction de chemins uniquement** (§6.3, §7.3) : `data/config.yaml` → `data/nommage/config.yaml`, exemple YAML mis à jour en objet nu. |
| 24/07/2026 | **v1.2** | erreurs_specs_v1.2.md | Claude | **Correction de chemins uniquement** (§6.1, codes `APP_CONFIG_INVALID`/`APP_CONFIG_MISSING`) : `data/config.yaml` → `data/core/config.yaml` (fichier socle) / `data/{app}/config.yaml` (section applicative), selon la source de l'erreur. |
| 24/07/2026 | **v1.8** | guide-nouvelle-application_specs_v1.8.md | Claude | **Corrections de références croisées uniquement**, suite au renommage `techniques-socle-ha-mqtt_specs_v4.13.md`→`v4.14.md` (restructuration `data/`). Aucun changement de contenu propre à ce guide. |
| 24/07/2026 | **v1.1** | alpinejs-implementation_specs_v1.1.md | Claude | **Correction de référence croisée uniquement**, suite au renommage `techniques-socle-ha-mqtt_specs_v4.13.md`→`v4.14.md`. Aucun changement de contenu propre à ce document. |
| 23/07/2026 | **v1.3** | fonctionnelles-ia_specs_v1.3.md | Claude | **Révision architecturale majeure**, issue d'une session de conception approfondie avec l'utilisateur : nouvelle §6 "Routage multi-IA" (le concept "VoiceRouter" existait déjà comme document de conception séparé, `voice-router-spec.md`, jamais implémenté — intégré ici) ; `ia` gagne un accès HA en lecture (`requiredHaWs: true`, était `false`) pour résoudre localement les outils de lecture ; `ia` déclare lui-même un jeu d'outils fixe à Mistral (§7) et exécute une vraie boucle d'appel d'outils avec résultats réinjectés (§8), pas un simple relais passif comme le disait la v1.2 ; l'action immédiate résolue par appel d'outil devient une planification immédiate non répétitive traitée par le point d'exécution unique de `planificateur` ; nouveaux événements `ia:tool:execute`/`:reply`. Sections renumérotées, toutes références croisées corrigées. À lire avec `fonctionnelles-planificateur_specs_v1.3.md`. |
| 23/07/2026 | **v1.3** | fonctionnelles-planificateur_specs_v1.3.md | Claude | **Révision majeure en miroir** : `resolved_service_call` n'est plus un placeholder inactif — mécanisme d'exécution **principal**, peuplé par `planificateur` (nouvelle §7 "Résolution des actions vers un service HA", table verbe→service à deux niveaux — services génériques HA pour les verbes sans valeur, services spécifiques par domaine sinon — prolongeant la table verbe→quoi des règles domotiques plutôt qu'une nouvelle table indépendante). §6 réécrite : trois déclencheurs (minuteur, macro directe, appel d'outil résolu) convergent vers le point d'exécution unique. §8 réécrite : exécution directe via `resolved_service_call` en chemin principal, agent de conversation HA relégué en repli pour les verbes non résolus. |
| 23/07/2026 | **v1.2** | fonctionnelles-ia_specs_v1.2.md | Claude | **Nouvelle §4 "Difficultés protocolaires connues"** (à la demande de l'utilisateur, qui se souvenait de vraies difficultés rencontrées lors de la mise au point d'un premier émulateur Ollama→Mistral) : balises markdown autour du JSON produit par Mistral, fragmentation des `tool_calls` en streaming (accumulation par index avant parsing), réconciliation des deux formats de requête Ollama (`/api/generate` legacy vs `/api/chat`), signal interne à ne pas laisser fuiter vers HA dans le flux NDJSON, en-tête anti-buffering nécessaire au streaming temps réel, forme d'erreur Ollama à produire en cas d'échec Mistral. Sections suivantes renumérotées (§4→§5 ... §10→§11, §11→§12), références croisées internes corrigées. Ancienne version v1.1 archivée. |
| 23/07/2026 | **v1.2** | fonctionnelles-planificateur_specs_v1.2.md | Claude | **Corrections de références croisées uniquement**, suite à la renumérotation de `fonctionnelles-ia_specs` en v1.2 (§5→§6, §6→§7, §7→§8) — les trois renvois correspondants (§2, §6, §8) mis à jour. Aucun changement de contenu propre à `planificateur`. Ancienne version v1.1 archivée. |
| 23/07/2026 | **v1.1** | fonctionnelles-planificateur_specs_v1.1.md | Claude | **Corrections issues d'une session de questions/réponses avec l'utilisateur avant implémentation** : §4 précise que `activer`/`desactiver`/`modifier` ne s'appliquent qu'aux planifications, jamais aux macros (pas d'état actif/inactif) ; §6 réécrite — la boucle de déploiement a en réalité deux déclencheurs possibles (minuteur de planification, ou macro nommée directement dans la conversation), qui convergent vers le même mécanisme et le même format `execution`, absent de la v1.0 qui ne décrivait que le cas du minuteur ; §3 précise que la résolution trigger→délai est un calcul déterministe en code, jamais fait par l'IA. Ancienne version v1.0 archivée. |
| 23/07/2026 | **v1.1** | fonctionnelles-ia_specs_v1.1.md | Claude | **Correction issue de la même session** : §5 clarifie que l'action immédiate (classification A) ne produit jamais de JSON structuré — traitée nativement par HA/Mistral (conversation ou `tool_calls`), ne transite jamais par `planificateur`. Le nœud `ActionNode` n'est qu'un sous-nœud à l'intérieur d'une macro/planification/séquence/exécution, jamais une réponse de premier niveau — absent de la v1.0. Ancienne version v1.0 archivée. |
| 23/07/2026 | **v1.0** | fonctionnelles-planificateur_specs_v1.0.md | Claude | **Nouvelle application PLANIFICATEUR** : stockage local (YAML) des macros/planifications domotiques, scheduler (7 types de déclencheurs), et — comportement structurant — réinterprétation systématique par `applications/ia` à chaque déclenchement plutôt qu'exécution d'une version pré-compilée (`phrase_originale` toujours source de vérité). Exécution des actions déléguée à l'agent de conversation HA (`HaWsClient.processConversation`, nouvelle méthode socle) ; `resolved_service_call` présent dans le schéma comme placeholder structurel volontairement inactif. À lire avec `fonctionnelles-ia_specs_v1.0.md`. |
| 23/07/2026 | **v1.0** | fonctionnelles-ia_specs_v1.0.md | Claude | **Nouvelle application IA** : émule le protocole HTTP d'Ollama (port 11434, serveur dédié indépendant du port web du socle) pour que l'intégration Ollama native de HA s'y adresse, relaie en réalité chaque requête vers l'API Mistral. Détecte le JSON structuré produit par Mistral et le route vers `applications/planificateur` via un mécanisme de requête/réponse interne à l'EventBus (identifiant de corrélation + timeout, écrit à la main — confirmé qu'aucun `InterAppClient` générique n'existe réellement dans le socle malgré `inter-app-communication_specs`). À lire avec `fonctionnelles-planificateur_specs_v1.0.md`. |
| 22/07/2026 | **v1.7** | guide-nouvelle-application_specs_v1.7.md | Claude | **Correction suite à la réintroduction d'Alpine.js** (audit de cohérence des specs) : la note historique v1.1 ("remplace Alpine.js") et le §1.2 ("TypeScript pur + Web Components natifs") laissaient croire Alpine.js toujours exclu. Alpine.js est désormais mentionné comme autorisé en complément, avec renvoi vers `alpinejs-implementation_specs_v1.0.md` qui fait autorité sur son cycle de vie. Références à `techniques-socle-ha-mqtt_specs` mises à jour v4.12→v4.13. |
| 22/07/2026 | **v1.0** | alpinejs-implementation_specs_v1.0.md | Claude | **Nouvelle spécification** : règles de chargement et de cycle de vie d'Alpine.js, à la demande de l'utilisateur suite à la réintroduction du framework (voir `presentation_specs_v4.0.md`). Documente exhaustivement ce qui a été validé via une démonstration interactive construite spécifiquement pour reproduire l'architecture réelle du socle (Shadow DOM comme `ModuleContainer.ts`, store partagé, composant récursif à 4 niveaux pour l'arbre OU/QUOI) : ne jamais appeler `Alpine.start()` manuellement (le build utilisé démarre seul via `queueMicrotask`, `window.deferLoadingAlpine` n'est pas respecté), toute déclaration via `alpine:init` dans la MÊME balise `<script>` que le chargement de la bibliothèque (une balise séparée arrive trop tard, la micro-tâche de démarrage s'intercale entre deux balises), `Alpine.initTree()` obligatoire après injection de contenu dans un Shadow DOM. |
| 22/07/2026 | **v4.0** | presentation_specs_v4.0.md | Claude | **Réintroduction d'Alpine.js**, retiré en v3.0 (16/07/2026) suite à un cycle de vie mal respecté par un assistant IA précédent (régressions à chaque modification ultérieure). Les règles de chargement/cycle de vie sont désormais dans le document dédié `alpinejs-implementation_specs_v1.0.md`, qui fait autorité sur le sujet — ce document reste la référence pour le reste (structure des répertoires, menu, communication Socket.io). Toutes les mentions "TypeScript pur, pas de framework" (périmètre §1.2, règles strictes §2.2, structure HTML §6.2, checklist §10.1) mises à jour en conséquence. Ancienne version v3.2 archivée. |
| 21/07/2026 | **v1.0** | fonctionnelles-evoo7_specs_v1.0.md | Claude | **Nouvelle application EVOO7** : intégration MQTT du régulateur de chauffage/PAC EVOO7 Control (VR Electronique), sur le principe RFXCOM/nommage (deux connexions MQTT indépendantes — broker EVOO7 propre à l'app, broker HA via le socle). 43 données seedées depuis `applications/evoo7/seed/evoo7_donnees.json`, modèle à 4 champs (`updatable`/`isConfigData` figés, `consultation`/`miseAJour` modifiables). **Limitation documentée** : les codes numériques des listes de valeurs (extraits de la page de config EVOO7) ne correspondent pas aux valeurs réellement observées sur le fil MQTT réel (vérifié par sniffing) — décision de sécurité : ces données restent en lecture seule (`sensor`, passthrough brut), jamais commandées, pour éviter d'envoyer une valeur potentiellement incorrecte au système réel. Validé en conditions réelles sur le broker EVOO7 et le broker HA de production. |
| 21/07/2026 | **v5.7** | fonctionnelles-rfxcom_specs_v5.7.md | Claude | **Scènes implémentées et testées** (§14.3) : le code n'existait pas malgré le "réactivées" annoncé en v5.4. Documente les noms d'événements Socket.io réels (`rfxcom:scenes:list`/`:list:get`, alignés sur la convention devices/récepteurs plutôt que le singulier initialement documenté) et deux simplifications connues (topic de découverte `device_automation` à un seul segment d'identifiant, non vérifié contre une instance HA réelle ; `SceneExecutionResult` ne distingue pas `scene_failed` de `scene_cancelled`). |
| 21/07/2026 | **v5.2** | recepteurs-emetteurs-rfxcom_specs_v5.2.md | Claude | **Implémentation réelle des Scènes** (§6, §10, §12.1) : corrige un défaut de conception hérité de v5.0 — `ReceiverSceneConfig` dérivait de `BaseReceiverConfig` et exigeait donc `primaryEmitter`/`emitters`, deux champs sans signification pour une scène, constaté en pratique (la validation Zod rejetait toute création de scène). `ReceiverSceneConfig` a désormais son propre schéma minimal. Architecture clarifiée : les scènes ne sont pas un 4ème `IReceiverModule`, elles sont gérées par un sous-système séparé `SceneManager`/`SceneExecutor` qui réutilise `ReceiverManager` pour appliquer chaque commande à son récepteur cible. Checklist §12.1 entièrement mise à jour (tout ✅). |
| 21/07/2026 | **v1.6** | guide-nouvelle-application_specs_v1.6.md | Claude | **Correction des exemples de chemins statiques (§3.8)** : le segment `presentation/` fait partie intégrante de l'URL servie (`/applications/{nom-app}/presentation/...`), les exemples l'omettaient (`/applications/{nom-app}/ts/app.js`) — confirmé erroné en pratique sur `nommage` et `arbreouquoi` (scripts/styles introuvables). Convention confirmée par `ModuleContainer.ts` du core et `presentation_specs` §4.3. Correction §5.1 : `AppService` ne scanne que `applications/`, pas de `dist/applications/` partagé à la racine. |
| 21/07/2026 | **v1.2** | fonctionnelles-nommage_specs_v1.2.md | Claude | **Correction** : le code v1.1 ne connectait/traitait en réalité que la première source (`couples[0]`) malgré la description — corrigé, `NommageMqttIntegrationService` gère désormais une `Map<sourceId, MqttClient>` réelle (§3.1). **Tableau de bord enrichi** (à la demande de l'utilisateur) : statut par connexion (nom + connecté/déconnecté) et entrées traitées par jour sur une fenêtre glissante de 5 jours (§3.5, §4.4, `NommageStatus.sources[]`/`.dailyCounts[]`, compteurs en mémoire non persistés). Correction des chemins statiques (`presentation/` fait partie de l'URL). |
| 21/07/2026 | **v1.2** | implementation-nommage_specs_v1.2.md | Claude | Réalignement des exemples de code §4.2/§4.4/§5.3 sur le code réel (ils étaient restés au design v1.0 malgré le texte v1.1 annonçant déjà le contraire) : `bridgeInstance: 'main'` obligatoire dans l'événement Passthrough (absent à tort de l'exemple v1.1), enregistrement du bridge au démarrage/arrêt, import `SocketService` corrigé (`ui-exports`, pas de singleton). Nouvelle §4.2.1 documentant `getSourceStatuses()` et le calcul glissant sur 5 jours (`getDailyCountsLastDays`). |
| 21/07/2026 | **v1.1** | fonctionnelles-nommage_specs_v1.1.md | Claude | **Sources MQTT multiples traitées simultanément** (à la demande utilisateur, vérification suite à l'audit du 21/07) : `mqtt` (objet unique) remplacé par `sources[]` (tableau), toutes connectées et traitées **en parallèle** (§3.1, §4.3) — cas d'usage type : lire simultanément le broker HA (topics `ha/...`) et Zigbee2MQTT sur le même broker mais préfixe différent (`homeassist/...`). **Transmission via le Passthrough MQTT du socle** (§3.4, §6, remplace l'ancien `nommage:transmit:to-core` vague) : NOMMAGE enrichit le message source et le relaie, sans reconstruire de message HA ni appeler l'API WebSocket. **Correction factuelle importante (§6.2)** : le référentiel `HaStructureRegistry` n'est **jamais** mis à jour à partir de messages MQTT — il est alimenté nativement par HA via notification WS, et peut ne pas exister du tout (seulement si `ha.ws_enable=true`) ; NOMMAGE ne dépend que de `ha.mqtt_enable`. Ancienne version v1.0 archivée. |
| 21/07/2026 | **v1.1** | implementation-nommage_specs_v1.1.md | Claude | `NommageMqttIntegrationService` gère désormais N connexions MQTT simultanées (une par source, Map de clients). Flux de transmission réécrit autour du Passthrough MQTT (`integration:nommage:passthrough:discovery`). Même correction sur l'indépendance du référentiel HA que ci-dessus. Ancienne version v1.0 archivée. |
| 21/07/2026 | **v5.1** | recepteurs-emetteurs-rfxcom_specs_v5.1.md | Claude | **Refonte des topics MQTT §8.5** (à la demande utilisateur, conforme au nouveau format défini dans `techniques-socle-ha-mqtt_specs_v4.9.md`) : un seul broker, nouveaux topics état/commande `/rfxcom/{bridgeInstance}/{deviceId}/state\|set`, LWT par `bridge_instance`, nouvel encodage RFXCOM du `deviceId` (§8.5.1 : `{protocole}_{sousProtocole}__{sensorId}_{unitCode}`, distinct de `unique_id`), topic de découverte HA standard inchangé (§7, mis à jour avec les nouveaux `state_topic`/`command_topic`), retrait des topics `rfxcom/scan` (superseded par Socket.io déjà documenté ailleurs), scène mise à jour vers le nouveau format. Ancienne version v5.0 archivée. |
| 21/07/2026 | **v5.6** | fonctionnelles-rfxcom_specs_v5.6.md | Claude | Alignement de `state_topic` (§7.2) et des topics de scène (§14.3.4) sur le nouveau format MQTT du socle. Ancienne version v5.5 archivée. |
| 21/07/2026 | **v1.1** | erreurs_specs_v1.1.md | Claude | Suppression de la référence active à Alpine.js dans le tableau de périmètre §1.2 (remplacée par une mention générique de la couche Présentation). Ancienne version v1.0 archivée. |
| 21/07/2026 | — | guide-nouvelle-application_specs_v1.4.md | Claude | **Renommage de correction** (pas de changement de contenu) : le fichier était nommé `v1.1.md` alors que son en-tête indiquait déjà `Version : 1.4`. Renommé pour cohérence ; références croisées corrigées dans les autres specs. |
| 21/07/2026 | — | architectural-patterns_specs_v1.0.md | Claude | **Renommage de correction** (pas de changement de contenu) : tiret avant `v1.0` remplacé par un underscore pour respecter la convention `[nom]_specs_v[X.Y]` appliquée partout ailleurs. |
| 21/07/2026 | — | presentation_specs_v3.2.md | Claude | **Renommage de correction** (pas de changement de contenu) : le fichier était resté nommé `v3.0.md` alors que son en-tête indiquait déjà `Version : 3.2` (les évolutions v3.1/v3.2 avaient été faites en place sans renommage ni archivage intermédiaire — gap d'historique pré-existant, non reconstituable). Renommé pour cohérence ; références croisées corrigées. |
| 21/07/2026 | — | fonctionnelles-arbreouquoi_specs_v1.0.md, fonctionnelles-nommage_specs_v1.0.md, implementation-arbreouquoi_specs_v1.0.md, implementation-nommage_specs_v1.0.md | Claude | **Correction de métadonnée** (pas de renommage, pas de nouveau contenu) : l'en-tête `Version :` indiquait `1.0.0` (format à 3 chiffres) alors que le nom de fichier et le reste du corpus utilisent `v1.0` (2 chiffres). Corrigé pour cohérence avec la convention effectivement appliquée partout ailleurs dans `specs/current/`. |
| 21/07/2026 | — | implementation-rfxcom_specs_v1.2.md | Claude | **Renommage de correction** (pas de changement de contenu fonctionnel) : le fichier était nommé `v1.0.md` alors que son en-tête indiquait déjà `Version 1.2` depuis le 17/07/2026 — incohérence déjà repérée dans la ligne du 17/07/2026 ci-dessous, jamais corrigée. Renommé pour cohérence ; ligne d'historique interne du document complétée. |
| 18/07/2026 | v5.4 | fonctionnelles-rfxcom_specs_v5.4.md | Mistral Vibe | Actions MQTT par le socle, gestion des protocoles RFXCOM |
| 17/07/2026 | v1.2 | implementation-rfxcom_specs_v1.2.md | Mistral Vibe | Démarrage automatique via AppService avec reconnexion sur changement de configuration, injection unifiée d'IAppConfigProvider, traces détaillées |
| 16/07/2026 | **v3.2** | presentation_specs_v3.2.md | Mistral Vibe | Comportement accordéon strict (1 seul menu déplié) et effacement systématique de la zone de données au clic. Correction du double déclenchement des écouteurs, utilisation de classes CSS `.visible` |
| 16/07/2026 | v3.1 | presentation_specs_v3.1.md | Mistral Vibe | Structure de menu accordéon et affichage conditionnel |
| 16/07/2026 | v3.0 | presentation_specs_v3.0.md | Mistral Vibe | Migration complète vers TypeScript pur, suppression de Alpine.js |
| 12/07/2026 | v1.0 | architectural-patterns_specs-v1.0.md | Mistral Vibe | Définition des patterns MQTT et WS pour l'intégration HA/MQTT |
| 12/07/2026 | v1.0 | integrationbridge-mqtt-auto_specs_v1.0.md | Mistral Vibe | Démarrage/arrêt automatique MQTT et HA-WS |
| 11/07/2026 | v1.0 | classification-rfxcom_specs_v1.0.md | Mistral Vibe | Classifieur RFXCOM (IHaClassifier) avec priorités subType → QUOI |
| 11/07/2026 | v1.0 | erreurs_specs_v1.0.md | Mistral Vibe | Codes d'erreur standardisés pour HA, MQTT, RFXCOM |
| 11/07/2026 | v1.0 | nommage_specs_v1.0.md | Mistral Vibe | Taxonomie QUOI/OÙ avec format `quoi---ou--ou` |
| 09/07/2026 | v5.0 | recepteurs-emetteurs-rfxcom_specs_v5.0.md | Mistral Vibe | Mapping N↔N, primaryEmitter, appairages dans YAML |
| 09/07/2026 | v5.0 | fonctionnelles-rfxcom_specs_v5.0.md | Mistral Vibe | Fichier de configuration centralisé, QUOI auto-déterminé |

---

## 🔍 Légende
- **Nouvelle version** : Mise en évidence avec `**vX.Y**`
- **Modifications majeures** : En gras dans la colonne "Changements"
- **Auteur** : "Mistral Vibe" pour les modifications automatiques, "-" pour les versions initiales
